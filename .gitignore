# ── macOS 目录元数据 ─────────────────────────────
.DS_Store
**/.DS_Store

# ── 嵌套仓库残留 ─────────────────────────────────
# 上传时把嵌套仓库的 .git 临时改成这个名字，不能传上去
.git-off/
**/.git-off/

# ── 一手论文与技术报告原文 ───────────────────────
# PDF 及其正文提取均为他人版权内容，本地保留、不进仓。
# 需要核对数字时从 arXiv / 官方渠道重新获取。
*.pdf
papers/
**/papers/

# ── 外部框架源码 ─────────────────────────────────
# Megatron-LM / vLLM / SGLang / DeepSpeed / veRL 等开源项目的完整拷贝，
# 仅作本地查阅，不重复上传。
源码/
**/源码/

# ── 编辑器备份 ───────────────────────────────────
*.bak
__pycache__/
*.pyc
