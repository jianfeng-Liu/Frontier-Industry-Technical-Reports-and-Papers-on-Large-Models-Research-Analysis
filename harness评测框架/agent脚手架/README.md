# agent 脚手架 · 总目录

> 面向新手的 **agent harness / scaffold（智能体脚手架）** 系统调研。
> 讲的是【包在模型外面、让它能真的动手干活的那层程序】——
> Claude Code、Codex 这类工具的"本事"绝大部分装在这一层里。

---

## ★★★ 先解决一个词：这里的 harness，和隔壁目录的 harness 不是一个东西

隔壁 [评测入门/](../评测入门/README.md) 讲的也叫 harness，但那是另一个意思。
**同一个英文单词，两个完全不同的程序。**

```
   ┌──────────────────────────────────────────────────────────────┐
   │  ★ evaluation harness（评测框架）                             │
   │    = 出题 + 判卷 的那个程序                                   │
   │    例：lm-evaluation-harness、SWE-bench 的 run_evaluation.py │
   │    ⟹ 隔壁目录 [评测入门/] 讲的是这个                          │
   ├──────────────────────────────────────────────────────────────┤
   │  ★ agent harness / scaffold（智能体脚手架）                   │
   │    = 让模型能读文件、跑命令、看报错、再改一遍 的那个程序      │
   │    例：SWE-agent、Claude Code、Codex CLI                     │
   │    ⟹ 🔑 本目录讲的是这个                                     │
   └──────────────────────────────────────────────────────────────┘

   ⟹ ★★★ 两者的接口窄到只有一个文件：
      脚手架跑完，产出一个补丁 → 评测框架收下这个补丁 → 判卷。
      ★★ 中间谁也不问谁。这就是为什么它们能被分成两个目录讲。
```

★★ 隔壁 [08 章](../评测入门/08-ClaudeCode与Codex怎么被评.md) 用 SWE-bench 源码证明了这一点：
判卷器只认 `instance_id` / `model_patch` / `model_name_or_path` 三个字段，
**补丁是谁生成的、跑了几轮、用了什么工具，它完全不问。**
本目录补的就是"它不问的那一段"。

---

## 阅读前：三个记号（与隔壁目录一致）

```
   ✅  = 论文原文 / 官方文档 / 源码原文可查证，我在正文里给了出处
   ⚠️  = 我的推断或补充说明，不是原文
   ★   = 重点（★ 一般 / ★★ 重要 / ★★★ 最重要）
   🔑  = 关键结论
   ⭐⭐ = 整章级别的重点章节
```

★★★ **一个必须先说清的限制：**

```
   ┌─ 本目录的可查证程度，分三档 ────────────────────────────────┐
   │                                                             │
   │  ✅ 全可查    SWE-agent  —— 有论文 + 开源实现，连消融实验   │
   │                            的每个数字都公开                 │
   │                                                             │
   │  ✅ 行为可查  Claude Code / Codex —— 闭源，但官方文档把     │
   │  ⚠️ 实现不可查            【配置项、模式名、工具名】写死了； │
   │                            内部代码怎么写的，查不到         │
   │                                                             │
   │  ✅ 全可查    DeepSeek —— ★★★ 报告披露了跑分那次的脚手架   │
   │              ＋源码       规格；2026 年 8 月又把脚手架本体   │
   │                            开源了（deepseek-harness，MIT）。 │
   │                            ⟹ 论文＋源码＋分数三样齐全，     │
   │                              是本目录可查证度最高的一家。    │
   │                            ⚠️ 但报告没说跑分用的就是这份     │
   │                              代码，沙箱平台 DSec 也未开源。  │
   └─────────────────────────────────────────────────────────────┘

   ⚠️ 这一档在 2026 年 8 月被改写过：原来 DeepSeek 排在"基本不可查"，
      开源之后直接跳到了最高档。详见 [07 章](07-DeepSeek的脚手架.md) §7.0。

   ⟹ 🔑 所以本目录不会假装讲得出 Claude Code 的源码。
      ★★ 讲得出的是：【它对外承诺了什么行为】——
      而这些行为，恰好就是影响榜单分数的那些变量。
```

---

## 全书骨架：一个脚手架由五件事组成

★★★ 这张表是后面七章的坐标系。左边五行，就是 [评测入门 08 章 §8.2](../评测入门/08-ClaudeCode与Codex怎么被评.md) 只列了名字、没有展开的那五个格子。

```
   ┌────────────────┬──────────────────────────┬──────────┐
   │ 组成部分        │ 一句话                    │ 本目录哪章│
   ├────────────────┼──────────────────────────┼──────────┤
   │ ① 循环          │ 模型说→执行→结果喂回去    │ 02       │
   │ ② 工具集 / ACI  │ 给模型几个什么动作        │ 03       │
   │ ③ 上下文管理    │ 旧的东西怎么扔掉          │ 04       │
   │ ④ 权限 / 沙箱   │ 允许它动到哪              │ 05       │
   │ ⑤ 采样 / 预算   │ 跑几遍、花多少钱才停      │ 08       │
   └────────────────┴──────────────────────────┴──────────┘

   ⟹ 🔑🔑 全目录定律：★★★
      【这五项每一项都能单独让 SWE-bench 分数动好几个点，
        而榜单上一项都不写。】
      ✅ 证据见 03 章那张 SWE-agent 消融表：换个搜索界面，18.0 → 12.0。
```

---

## 章节目录

| 章 | 标题 | 一句话 | 重要度 |
|---|---|---|---|
| 01 | [两个 harness 不是一个东西](01-两个harness不是一个东西.md) | 划清边界：脚手架从哪开始、到哪结束 | ★★ |
| 02 | [agent 循环怎么转](02-agent循环怎么转.md) | ⭐ 最基础的一章。工具调用循环 + ReAct + 停止条件 | ★★★ |
| 03 | [工具集与 ACI](03-工具集与ACI.md) | ⭐⭐ ✅ 全是 SWE-agent 论文实测数字：界面一换，分数掉 6 分 | ★★★ |
| 04 | [上下文管理](04-上下文管理.md) | ⭐⭐ ✅ 反直觉：把全部历史留给模型，分数反而掉 3 分 | ★★★ |
| 05 | [权限与沙箱](05-权限与沙箱.md) | ✅ Claude Code 五种模式 / Codex 三种沙箱三种审批，逐个对照 | ★★ |
| 06 | [Claude Code 与 Codex 逐项对照](06-ClaudeCode与Codex逐项对照.md) | ⭐⭐ 把两个产品公开可查的部分摆在一张表里 | ★★★ |
| 07 | [DeepSeek 的脚手架](07-DeepSeek的脚手架.md) | ✅ 唯一逐项公开跑分脚手架规格的一家：2 个工具 / 500 步 / 512K 上下文；★★★ 工具结果挂在哪个角色下会换模型档位 | ★★★ |
| 08 | [脚手架怎么影响榜单分数](08-脚手架怎么影响榜单分数.md) | ⭐⭐ ★★★ 收口章，接回评测入门 08 和 11 | ★★★ |

📋 另有 [附-速查表.md](附-速查表.md) —— 术语全称、配置项名、关键数字集中在一页。

---

## 如果你时间有限

```
   ┌─ 只有 15 分钟 ────────────────────────────────────────┐
   │  读 [02 agent 循环怎么转] + [08 脚手架怎么影响分数]    │
   │  ⟹ 够你看懂"为什么同一个模型换个壳分数差一倍"。       │
   └───────────────────────────────────────────────────────┘
   ┌─ 你要自己搭一个 agent ────────────────────────────────┐
   │  读 [02] → [03] → [04]，这三章是全部技术核心。        │
   │  ⟹ 🔑 03 和 04 的每个结论都有实测数字撑着。          │
   └───────────────────────────────────────────────────────┘
   ┌─ 你要在公司里部署 Claude Code / Codex ────────────────┐
   │  直接读 [05 权限与沙箱] + [06 逐项对照]。             │
   └───────────────────────────────────────────────────────┘
   ┌─ 你在写评测报告，要说清"我们的脚手架是什么" ───────────┐
   │  读 [08.6 那张披露清单]，照着填。                     │
   └───────────────────────────────────────────────────────┘
```

---

## 全目录最重要的五条

```
   ① ★★★ 脚手架不是"包装"，它本身就是能力的一部分。
      ✅ 证据：SWE-agent 论文标题就是这句话——
         "Agent-Computer Interfaces Enable Automated Software Engineering"
         （智能体-计算机接口，使自动化软件工程成为可能）

   ② ★★★ 界面设计的每一个细节都值好几个点。
      ✅ 证据：SWE-bench Lite 上，同一个 GPT-4 Turbo：
         编辑器带语法检查 18.0，不带 15.0，完全没有编辑命令 10.3。

   ③ ★★★ 给模型看得越多 ≠ 效果越好。
      ✅ 证据：文件视图一次给 100 行 = 18.0，给 30 行 = 14.3，
         给整个文件 = 12.7。★★ 两头都掉。

   ④ ★★★ 上下文要主动扔，不是留着就好。
      ✅ 证据：只保留最近 5 条观察 = 18.0，保留全部历史 = 15.0。

   ⑤ ★★★ 闭源产品的脚手架细节，公开的只有【配置面】，没有【实现面】。
      ✅ 证据：DeepSeek-V3.1 模型卡原话
         "SWE-bench is evaluated with our internal code agent framework."
         （SWE-bench 是用我们内部的 code agent 框架评测的。）
         ⟹ 承认了有，也就仅此而已。
```

---

## 一手资料清单

★★ 本目录所有 ✅ 标记的引用，出处如下。

### 论文（`../papers/`，已下载并通过 pypdf 完整性校验）

| 文件 | 页数 | 用在 |
|---|---|---|
| `SWE-agent-arXiv-2405.15793.pdf` | 118 | 02、03、04、08 —— 本目录唯一一份【全可查证】的脚手架 |
| `SWE-bench-arXiv-2310.06770.pdf` | 52 | 01、08 |

### 官方文档（联网查证，非本地归档）

| 来源 | 用在 |
|---|---|
| Claude Code 官方文档 `code.claude.com/docs/en/` —— `permission-modes` / `sub-agents` / `costs` / `settings` | 04、05、06 |
| Codex 官方文档 `learn.chatgpt.com/docs/` —— `sandboxing` / `config-file/config-reference` / `open-source` | 05、06 |
| DeepSeek-V3.1 模型卡 `huggingface.co/deepseek-ai/DeepSeek-V3.1` | 07 |

⚠️ 说明：官方文档是【会变的】。上面这些页面的内容以 2026-08-27 查阅为准，
配置项名称和模式名可能随版本调整。★★ 论文里的数字不会变，文档里的名字会变。

---

> 上级目录：[harness评测框架/](../) ｜ 姊妹目录：[评测入门/](../评测入门/README.md)
