# 附 · 速查表

> ★★ 全部 8 章的术语、配置项名、关键数字集中在这一页。
> ⟹ 建议：正文读完一遍之后，日常只翻这一页。
>
> ⚠️ 记号：✅ = 一手来源可查证 ｜ ⚠️ = 推断或未能核实
> ★ / ★★ / ★★★ = 重要程度

---

## A. 术语全表（按首次出现章节排序）

| 术语 | 全称 / 字面意思 | 一句话 | 章 |
|---|---|---|---|
| **harness** ⚠️ | 挽具 | ★★★ **同音异义**：既指评测框架，也指 agent 脚手架 | 01 |
| **evaluation harness** | 评测框架 | 跑题目、判对错的那个程序 ⟹ 讲它的是[评测入门](../评测入门/README.md) | 01 |
| **agent harness / scaffold** | 智能体脚手架 | 包在模型外面那层循环、工具、提示词 ⟹ 本目录讲它 | 01 |
| **agent** | 智能体 | 能自己多轮决策、动手操作环境的系统 | 01 |
| **patch / diff** | 补丁 / 差异 | ★★★ 脚手架和评测框架之间**唯一**的接口 | 01 |
| **token** | 词元 | 模型处理文本的最小单位 | 01 |
| **tool** | 工具 | 脚手架提供给模型的一个可调用动作 | 02 |
| **tool call / function call** | 工具调用 / 函数调用 | 模型吐出的"我要调用 X，参数是 Y" | 02 |
| **tool result** | 工具结果 | ★★★ 执行后拼回对话的那条消息（见 07 章：挂在哪个角色下有后果） | 02 |
| **ReAct** | Reason + Act（推理 + 行动） | 每一步先想再动的范式 | 02 |
| **trajectory** | 轨迹 | 一次任务里全部"思考+动作+结果"的完整序列 | 02 |
| **turn / step** | 轮 / 步 | 循环转一圈 | 02 |
| **malformed generation** | 格式错误的生成 | 模型吐出的东西解析不了 | 02 |
| **exit_cost** | 成本退出 | ★★ 花钱超上限而中止 | 02 |
| **submit** | 提交 | 模型主动宣告做完了 | 02 |
| **ACI** | Agent-Computer Interface（智能体-计算机接口） | ★★★ 给模型用的界面，区别于给人用的 UI | 03 |
| **guardrail** | 护栏 | ★★ 工具内部的检查，挡下无效操作（如 linter） | 03 |
| **linter** | 代码检查器 | 不运行代码就能查出语法错的工具 | 03 |
| **ablation** | 消融实验 | ★★★ 拆掉一个部件，看分数掉多少 | 03 |
| **context window** | 上下文窗口 | 模型一次能"看见"的文字总量上限 | 04 |
| **history processor** | 历史处理器 | ★★ 决定哪些历史保留、哪些折叠的组件 | 04 |
| **truncate / collapse** | 截断 / 折叠 | 上下文策略①：删掉或压成一行 | 04 |
| **compact / compaction** | 压缩 | 上下文策略②：★★ 再调一次模型做总结 | 04 |
| **isolate** | 隔离 | 上下文策略③：★★★ 丢给独立上下文的子 agent | 04 |
| **prompt caching** | 提示词缓存 | ⚠️ 只省钱，不省 token | 04 |
| **subagent** | 子 agent | 独立上下文窗口的委派 agent | 04/06 |
| **permission** | 许可 | ★★★ 执行【前】问不问你（拦命令文本） | 05 |
| **sandbox** | 沙箱 | ★★★ 执行【时】够不够得着（操作系统内核拦） | 05 |
| **classifier** | 分类器 | ★★★ 用另一个模型替你审查动作 | 05 |
| **Seatbelt** | — | macOS 自带的进程沙箱框架 | 05 |
| **bubblewrap / bwrap** | — | Linux 上的无特权沙箱工具 | 05 |
| **seccomp** | secure computing mode | Linux 内核按系统调用粒度过滤 | 05 |
| **WSL2** | Windows Subsystem for Linux 2 | Windows 里跑 Linux 的官方方案 | 05 |
| **ephemeral container** | 一次性容器 | ★★ 跑完就销毁的容器 | 05 |
| **escape hatch** | 逃生舱 | ★★ 沙箱挡住时降级到常规许可流程重试 | 05 |
| **hook** | 钩子 | ★★★ 固定生命周期节点上自动执行的脚本，**强制生效** | 06 |
| **MCP** | Model Context Protocol（模型上下文协议） | 让第三方把工具接进 agent 的开放协议 | 06 |
| **managed settings** | 托管设置 | ★★ 公司下发、个人改不掉的配置 | 06 |
| **agent thread** | agent 线程 | Codex 里子 agent 干活的那条线程 | 06 |
| **interleaved thinking** | 交错思考 | ★★★ 思考与工具调用交替，且思考轨迹在后续轮次仍可见 | 07 |
| **thinking / reasoning trace** | 思考轨迹 | 模型给出动作前那段"内心独白" | 07 |
| **discard-all** | 全部丢弃 | ★★ 最激进的上下文截断策略 | 07 |
| **DSec** | DeepSeek Elastic Compute | DeepSeek 的生产级沙箱平台 | 07 |
| **microVM / fullVM** | 微虚拟机 / 全虚拟机 | 基于 Firecracker / QEMU 的更强隔离 | 07 |
| **deterministic replay** | 确定性重放 | ★★ 从轨迹日志忠实复现历史会话 | 07 |

---

## B. 脚手架五大部件对照卡

```
   ┌───┬──────────────┬────────────────────────────┬────┐
   │ ① │ 循环          │ 什么时候停？几步？花多少钱？│ 02 │
   │ ② │ 工具集 / ACI  │ ★★★ 给几个工具？好不好用？ │ 03 │
   │ ③ │ 上下文管理    │ ★★★ 扔什么？留什么？压缩吗？│ 04 │
   │ ④ │ 权限 / 沙箱   │ 谁拦？拦在哪一层？          │ 05 │
   │ ⑤ │ 采样 / 预算   │ 温度？跑几遍？上限多少？    │ 02 │
   └───┴──────────────┴────────────────────────────┴────┘
```

---

## C. ✅ 值得记住的关键数字

**★★★ SWE-agent 消融（GPT-4 Turbo，SWE-bench Lite，%Resolved）**

| 配置 | 分数 | 差值 |
|---|---|---|
| 完整 SWE-agent | **18.0** | — |
| 换掉文件编辑器界面 | 10.3 | ↓ **7.7** ★★★ |
| 搜索改成迭代式 | 12.0 | ↓ 6.0 ★★★ |
| 留全部历史（不折叠） | 15.0 | ↓ **3.0** ★★★ |
| 编辑器去掉 linter 护栏 | 15.0 | ↓ 3.0 |
| ★★ 完全不给搜索工具 | 15.7 | ↓ 2.3 ⚠️ **比"给难用工具"还好** |

**★★ linter 护栏的经济账**

| | 数值 |
|---|---|
| 无效编辑的比例 | 51.7% |
| 护栏拦下其中多少 | 90.5% |
| 拦下后重试成功率 | 57.2% |

**★★ SWE-agent 成功 vs 失败的成本**

| | 平均花费 | 平均步数 | 该组解决率 |
|---|---|---|---|
| 成功 | $1.21 | 约 12 步 | 93.0% |
| 失败 | $2.52 | 约 21 步 | 69.0% |

⟹ ★★ 论文概括：**"Agents succeed quickly and fail slowly."**（agent 成功得快，失败得慢。）

**★★★ SWE-bench 仓库规模（论文 Table 1）**

| | 均值 | 最大 |
|---|---|---|
| 非测试文件数 | 3,010 | 5,890 |
| 非测试代码行数 | **438K** | 886K |
| issue 长度（词） | 195.1 | 4,477 |
| ★★★ 标准答案改了几行 | **32.8** | 5,888 |

⟹ 🔑 **438,000 行里只改 32.8 行 ⟹ 99.99% 是噪音。**

**★★★ DeepSeek-V4 跑分脚手架规格（报告 §5.3.1）**

| 项 | 值 |
|---|---|
| 工具集 | ★★★ **2 个**：bash + 文件编辑 |
| 最大交互步数 | **500** |
| 最大上下文 | **512K token** |
| 框架 | ✅ 内部自研；⚠️ 报告未指名，但同名脚手架 `deepseek-harness`（dsh）已于 2026-08 开源（MIT） |
| BrowseComp 上下文策略 | discard-all（全部丢弃） |

**★★ 开源脚手架 dsh 对照（✅ 源码，2026-09-04 拉取的 master）**

| 项 | 值 |
|---|---|
| 仓库 / 协议 | `deepseek-ai/deepseek-harness`，MIT，Node.js，developer preview |
| 跑分档位 | profile `sdk-minimal`，官方自称 "two-tool profile" |
| 那两个工具 | `dsh-tool-bash-persistent`（超时 300 秒）+ `str_replace_editor`（输出上限 16000 字符） |
| 系统提示词 | 一句话：`You are a helpful software engineer assistant.` |
| 工具目录总量 | ✅ 62 个 model-visible 工具，24 个工具包 ⟹ 跑分档位只留 2 个 |
| 500 步上限 | ❌ 仓库里没有这个开关（`maxSteps` 零命中）⟹ 属于跑分驱动脚本层 |
| 沙箱 | ⚠️ 只有同机进程级（bwrap / Landlock / Seatbelt / Windows ACL）；报告里的 DSec 未开源 |
| 思考轨迹 | ✅ 适配器无条件"原样回传上一回合的推理内容，无论那回合有没有调工具" |

**DeepSeek-V4 报告 Table 6 · SWE Verified（Resolved %）**

| Opus-4.6 Max | Gemini-3.1-Pro High | K2.6 Thinking | DS-V4-Pro Max |
|---|---|---|---|
| 80.8 | 80.6 | 80.2 | 80.6 |

⚠️ **读这张表前必须知道**：报告只说明了 DeepSeek 自己用的脚手架规格，
未说明其他模型用的是什么脚手架。

---

## D. Claude Code 配置项速查（✅ 全部来自官方文档）

**许可模式**（`permissions.defaultMode`，或 `--permission-mode`）

| 值 | 不问就能干 |
|---|---|
| `default`（界面名 **Manual**） | 只读 |
| `acceptEdits` | 读 + 改文件 + 常见文件系统命令 |
| `plan` | 读（+ auto 可用时的分类器批准命令） |
| `auto` | 全部，有后台安全检查 |
| `dontAsk` | 只有预先批准的工具 |
| `bypassPermissions` | ⚠️ 全部，无检查 |

⟹ ★★ 会话中切换：`Shift+Tab`。★★★ 任何模式都不放行 `rm` 关键路径。

**沙箱**

| 配置键 / 命令 | 作用 |
|---|---|
| `/sandbox` | 打开沙箱面板 |
| `sandbox.enabled` | 全局开启 |
| `sandbox.failIfUnavailable` | 沙箱起不来时硬失败 |
| `sandbox.allowUnsandboxedCommands: false` | ★★ 严格沙箱模式，关掉逃生舱 |
| `sandbox.filesystem.allowWrite` / `denyWrite` / `allowRead` / `denyRead` | 文件边界 |
| `sandbox.network.allowedDomains` / `deniedDomains` / `strictAllowlist` | 网络边界 |
| `sandbox.excludedCommands` | 完全排除在沙箱外的命令 |
| `dangerouslyDisableSandbox` | ★★ 单次逃生舱参数 |

⟹ ★ 默认边界：只能写当前工作目录 + 会话临时目录；★★★ **网络默认不放行任何域名**。
⟹ ⚠️ 不支持原生 Windows，需 WSL2。依赖 `bubblewrap` + `socat`（+ 可选 seccomp）。

**上下文 / 记忆**

| 命令 / 路径 | 作用 |
|---|---|
| `/compact` / `/compact <重点>` | 手动压缩 |
| `/clear` | 清空重开 |
| `/context` | 看上下文占用 |
| `/memory` | 浏览编辑记忆文件 |
| `CLAUDE.md` 里 `# Compact instructions` | 固化压缩偏好 |
| `~/.claude/projects/<项目>/memory/` | 自动记忆目录 |
| `claudeMdExcludes` | 排除无关的 CLAUDE.md |

**CLAUDE.md 位置（加载顺序：广 → 窄）**

1. 托管策略：macOS `/Library/Application Support/ClaudeCode/CLAUDE.md`；Linux/WSL `/etc/claude-code/CLAUDE.md`；Windows `C:\Program Files\ClaudeCode\CLAUDE.md`
2. 用户：`~/.claude/CLAUDE.md`
3. 项目：`./CLAUDE.md` 或 `./.claude/CLAUDE.md`
4. 本地：`./CLAUDE.local.md`

⟹ ★★ 引入其他文件：`@path/to/file`（最多 4 层）。兼容 AGENTS.md：`@AGENTS.md` 或 `ln -s AGENTS.md CLAUDE.md`。
⟹ ★ 建议每个文件 200 行内；超过 4 MiB 直接跳过。

---

## E. Codex 配置项速查（✅ 全部来自官方文档，写在 `config.toml`）

**沙箱与审批**

| 配置键 | 取值 |
|---|---|
| `sandbox_mode` | `read-only` / `workspace-write` / `danger-full-access` |
| `approval_policy` | `untrusted` / `on-request` / `never` |
| `approvals_reviewer` | `user`（默认） / `auto_review` |
| `sandbox_workspace_write.writable_roots` | 扩展可写目录 |

⟹ ★★ 命令行等价：`codex --sandbox workspace-write --ask-for-approval on-request`
⟹ ⚠️ `auto_review` 只改审批路由，**不改沙箱边界**。

**上下文**

| 配置键 | 作用 |
|---|---|
| `model_context_window` | 上下文 token 数 |
| `model_auto_compact_token_limit` | ★★★ 触发自动压缩的阈值 |
| `model_auto_compact_token_limit_scope` | `total` / `body_after_prefix` |
| `compact_prompt` | 替换压缩提示词 |
| `experimental_compact_prompt_file` | 同上，从文件读 |
| `tool_output_token_limit` | ★★★ 单条工具输出保留上限 |

**子 agent**

| 配置键 | 作用 |
|---|---|
| `agents.enabled` | 总开关（默认 `true`） |
| `agents.max_concurrent_threads_per_session` | 并发上限 |
| `agents.default_subagent_model` | 默认模型 |
| `agents.default_subagent_reasoning_effort` | 默认思考强度 |
| `agents.interrupt_message` | 中断时记录一条模型可见消息 |

⟹ ★★ 定义位置：`~/.codex/agents/`（个人）或 `.codex/agents/`（项目），**一个 agent 一个 TOML 文件**。
⟹ ★ 必填字段：`name`、`description`、`developer_instructions`。
⟹ ★★ 内置三个：`default` / `worker` / `explorer`。

**AGENTS.md**

- 全局：`~/.codex`（或 `CODEX_HOME`）下先试 `AGENTS.override.md`，回退 `AGENTS.md`
- 项目：从 Git 根逐级向下走到当前目录，每层先试 `AGENTS.override.md` 再试 `AGENTS.md`
- 合并：从根往下拼接，★★ **越靠近当前目录的越晚出现，因而覆盖前面的**
- 上限：`project_doc_max_bytes`，**默认 32 KiB**
- 备选文件名：`project_doc_fallback_filenames`

---

## F. ⚠️⚠️ 四个容易踩的坑

### ① ★★★ 「harness」是同音异义词

```
   评测入门讲的 harness = 判卷程序
   本目录讲的 harness = agent 脚手架
   ⟹ ⚠️ DeepSeek-V4 报告里两个意思都出现了，要看上下文。
```

### ② ★★★ 坏工具比没工具更糟

```
   ✅ 不给搜索工具 = 15.7
   ✅ 给一个难用的迭代式搜索 = 12.0
   ⟹ 🔑 模型会去用坏工具，然后被带偏、被它填满上下文。
```

### ③ ★★★ 工具结果挂在哪个角色下，是有后果的

```
   ✅ DeepSeek-V4 报告：
      作为 tool result 回传 ⟹ 触发工具调用路径 ⟹ 思考轨迹保留
      伪装成 user message ⟹ 不触发 ⟹ 官方建议改用非思考模型
   ⟹ 🔑 ★★★ 同一个模型，脚手架写法不同 = 换一个档位。
```

### ④ ★★★ 榜单上没有"模型的分数"

```
   只有【模型 + 脚手架 + 子集 + 预算 + 口径】这一整套系统的分数。
   ⚠️ 评测框架能看到的只有一个 patch，它不知道脚手架长什么样。
```

---

## G. ★★★ 看到一个 agentic 分数，追问"什么脚手架"时展开成七问

```
   ┌────┬────────────────────┬────┐
   │②a  │ 工具集是什么？几个？有护栏吗？ │ 03 │
   │②b  │ 上下文策略？留几条？窗口多大？ │ 04 │
   │②c  │ 步数上限多少？                 │ 02 │
   │②d  │ 成本上限多少？                 │ 02 │
   │②e  │ ★★★ 许可档位是哪一档？        │ 05 │
   │②f  │ ★★★ 工具结果怎么回传？        │ 07 │
   │②g  │ 用没用子 agent？               │ 06 │
   └────┴────────────────────┴────┘

   ⚠️ 现实：②e 至今没有任何一家披露过。
```

⟹ ★★ 另外六件事（子集 / 脚手架 / 尝试次数 / 挑选是否看测试 / 预算 / 模型版本）
见 [评测入门 08.7](../评测入门/08-ClaudeCode与Codex怎么被评.md)。

---

## H. 一手来源清单

| 来源 | 位置 | 用在 |
|---|---|---|
| ✅ SWE-agent 论文 arXiv:2405.15793 | [`papers/SWE-agent-arXiv-2405.15793.pdf`](../papers/SWE-agent-arXiv-2405.15793.pdf) | 01–05、08 |
| ✅ SWE-bench 论文 arXiv:2310.06770 | [`papers/SWE-bench-arXiv-2310.06770.pdf`](../papers/SWE-bench-arXiv-2310.06770.pdf) | 04 |
| ✅ DeepSeek-V4 技术报告 arXiv:2606.19348 | [`业界模型技术报告/papers/arxiv-2606.19348.pdf`](../../业界模型技术报告/papers/arxiv-2606.19348.pdf) | 07 |
| ✅ Claude Code 官方文档 | `code.claude.com/docs/en/` — permission-modes / sandboxing / settings / memory / sub-agents / costs | 04–06 |
| ✅ Codex 官方文档 | `learn.chatgpt.com/docs/` — sandboxing / config-file/config-reference / agent-configuration/agents-md / agent-configuration/subagents / open-source / hooks | 04–06 |

⚠️ **提醒**：论文里的数字不会变，**官网文档会变**。
本目录的引用截至 **2026-08-27**。发现对不上时，以官网当前版本为准。

---

> 回到 [总目录](README.md) ｜ 姊妹目录 [评测入门](../评测入门/README.md)
