# 附 · 速查表

> ★★ 全部 12 章的术语、清单、关键数字集中在这一页。
> ⟹ 建议：正文读完一遍之后，日常只翻这一页。

---

## A. 术语全表（按首次出现章节排序）

| 术语 | 全称 / 字面意思 | 一句话 | 章 |
|---|---|---|---|
| **harness** | evaluation harness（评测框架 / 挽具） | 跑评测题目的那套程序 | 01 |
| **benchmark** | 基准 / 评测集 | 题目本身（MMLU、GSM8K…） | 01 |
| **metric** | 指标 | 怎么把对错算成一个数 | 01 |
| **leaderboard** | 榜单 | 把很多分数排在一起的表 | 01 |
| **judge-free** | 无需裁判 | 靠规则/概率就能判，不用另一个模型 | 02 |
| **loglikelihood** | 对数似然 | 模型认为某段文字有多"像话" | 02 |
| **perplexity** | 困惑度 | 由 loglikelihood 换算，越低越"不意外" | 02 |
| **BPB** | Bits-Per-Byte（每字节比特数） | 用字节当分母，★ 跨分词器可比 | 02/10 |
| **generate_until** | 生成到某个停止符为止 | 三原语之一，让模型自由写 | 03 |
| **loglikelihood_rolling** | 滚动对数似然 | 三原语之一，算整段文本的似然 | 03 |
| **narrow waist** | 窄腰设计 | 中间层只有极少几种抽象，上下各自演化 | 03 |
| **Instance** | 实例 | 一次"模型请求"的数据结构，贯穿全流程 | 03 |
| **few-shot** | 少样本 | 提示词里先塞几道例题 | 04 |
| **zero-shot** | 零样本 | 不给例题，直接问 | 04 |
| **chat template** | 对话模板 | 把 messages 拼成模型认识的字符串的规则 | 04 |
| **CoT** | Chain-of-Thought（思维链） | 让模型先写推理过程再给答案 | 04 |
| **filter / extraction** | 过滤器 / 抽取 | 从自由文本里把答案抠出来 | 05 |
| **exact_match** | 精确匹配 | 抠出来的串和标准答案逐字相等才算对 | 05 |
| **execution-based** | 执行验证 | 不看文本像不像，★★ 真跑一遍看结果 | 05/07 |
| **pass@k** | k 次通过率 | ⚠️ 有两种定义，见 D 节 | 05/10 |
| **contamination** | 数据污染 | 评测题进了训练数据 | 06 |
| **n-gram 匹配** | n 元组匹配 | 用连续 n 个词去查重，常用 10-gram | 06 |
| **Goodhart's Law** | 古德哈特定律 | 指标一旦成为目标，就不再是好指标 | 06 |
| **agentic** | 智能体式 | 模型自己动手操作环境、多轮试错 | 07 |
| **issue / PR / patch** | 问题单 / 合并请求 / 补丁 | GitHub 上的三样东西 | 07 |
| **FAIL_TO_PASS** | 原本失败→应该变通过 | 尺子①：★★ 这个 bug 修好了吗 | 07 |
| **PASS_TO_PASS** | 原本通过→必须还通过 | 尺子②：★★★ 有没有搞坏别的 | 07 |
| **scaffold** | 脚手架 | 包在模型外面那层循环、工具、提示词 | 08 |
| **ACI** | Agent-Computer Interface（智能体-计算机界面） | 给模型用的界面，区别于给人用的 UI | 08 |
| **guardrail** | 护栏 | 提交前先检查，不通过就拒绝这次操作 | 08 |
| **ablation** | 消融实验 | 拆掉某个部件，看分数掉多少 | 08 |
| **LLM-as-a-Judge** | 让模型当裁判 | 用另一个模型给回答打分 | 09 |
| **pairwise / single-answer / reference-guided** | 两两对比 / 单答打分 / 带参考答案 | 三种裁判形态 | 09 |
| **position bias** | 位置偏差 | 裁判偏爱排在前面的那个 | 09 |
| **verbosity bias** | 冗长偏差 | 裁判偏爱更长的回答 | 09 |
| **self-enhancement bias** | 自我偏好偏差 | 裁判偏爱自己家族的回答 | 09 |
| **Elo** | Elo 等级分（源自国际象棋） | 把两两胜负换算成一个排名分 | 09 |
| **bootstrap** | 自助法 | 反复重采样估计置信区间 | 09 |
| **anchor model** | 锚点模型 | 所有人都和它比，避免 N² 次对战 | 09 |
| **noise floor** | 噪声下限 | 小于这个差距就算平手 | 10 |
| **greedy decoding** | 贪心解码 | 每步取概率最高的词，T=0 | 10 |
| **temperature / top-p** | 温度 / 核采样 | 控制随机程度的两个旋钮 | 10 |
| **cons@k** | consensus@k（多数投票） | 采样 k 次，取出现最多的答案 | 10 |
| **rule-based RM** | 基于规则的奖励模型 | 训练时用规则而不是模型给奖励 | 10 |
| **gold answer** | 黄金答案 | 评测时对照的正确参考 | 12 |

---

## B. 三种范式对照卡

```
┌────────────┬─────────────┬──────────────┬─────────────┬──────────┐
│            │ ① 判别式    │ ② 生成式     │ ③ agentic   │          │
├────────────┼─────────────┼──────────────┼─────────────┼──────────┤
│ 模型输出   │ 不输出       │ 一段文本      │ 多轮动作     │          │
│ 怎么判     │ 比概率       │ 抽取 + 匹配   │ 真跑测试     │          │
│ 需要抽取吗 │ ❌ 不需要   │ ✅ 需要 ★★★ │ ❌ 不需要   │          │
│ 闭源 API   │ ⚠️ 做不了   │ ✅ 可以      │ ✅ 可以     │          │
│ 单题成本   │ 极低         │ 低           │ ★★★ 极高   │          │
│ 可复现性   │ 高           │ 中           │ 低           │          │
│ 典型基准   │ MMLU        │ GSM8K/MATH   │ SWE-bench   │          │
└────────────┴─────────────┴──────────────┴─────────────┴──────────┘

⟹ 🔑 定律：★★★【越真实 ⟺ 越贵 ⟺ 越难复现】

⚠️ 那个"闭源 API 做不了判别式"的格子最容易被忽略：
   ★★★ GPT/Claude 的 API 不返回 logprobs ⟹ 无法做判别式评测
   ⟹ 同一张榜单上的开源模型和闭源模型，可能【口径根本不同】。
```

---

## C. 看到一个分数，问这七件事（11 章速用卡）

```
   ① ★★ 这是哪种范式？（判别式 / 生成式 / agentic）
   ② ★★ few-shot 几 shot？用了什么提示词模板？
   ③ ★★★ 生成式的话：抽取规则是什么？抽取失败算什么？
   ④ ★★★ agentic 的话：脚手架是什么？跑了几次？怎么挑的？
   ⑤ ★★ 温度多少？跑几遍？pass@1 是哪种定义？
   ⑥ ★★ 这个数是自己跑的，还是从别人报告里抄的？
   ⑦ ★★★ 这个基准发布于该模型训练截止日之前吗？（污染）

   ⟹ 🔑 七个里答不上三个以上，这个分数只能当【非常粗的参考】。
```

---

## D. ⚠️⚠️ 三个"同名不同义"的陷阱

★★★ 这三个是全书最容易踩的坑，单独拎出来。

### ① pass@1 有两种完全不同的定义

```
   ┌─ HumanEval 那一系（取最大值语义）───────────────────┐
   │  pass@k = 采样 k 个，★ 只要有一个对就算对            │
   │  ⟹ k 越大分数越高。pass@1 = 只采一个。              │
   └──────────────────────────────────────────────────────┘
   ┌─ DeepSeek-R1 那一系（取平均语义）────────────────────┐
   │  pass@1 = (1/k)·Σ pᵢ ，★★ k 次各自对错取【平均】     │
   │  ⟹ ✅ R1 用 k=64（AIME/GPQA）、16（MATH/CF）、8（LCB）│
   │  ⟹ k 越大分数【不变高，只变稳】                      │
   └──────────────────────────────────────────────────────┘

   ⚠️ 🔑 两张榜单都写 "pass@1"，一个是 greedy 跑一次，
      一个是采样 64 次取平均 ⟹ ★★★【这两个数不能直接比】。
```

### ② "可复现"有两种

```
   ① 逐次可复现（同输入 → 同输出）   ← greedy 提供这个
   ② 统计可复现（同配置 → 同分数）   ← 多次采样取平均提供这个

   ✅ R1 报告的发现：长输出推理模型用 greedy 会
      "higher repetition rates and significant variability
       across different checkpoints"
   ⟹ 🔑 ★★★ 对长输出模型，② 比 ① 重要得多。
```

### ③ SWE-bench 有三个不同的题集

```
   ┌──────────────┬────────┬──────────────────────────────┐
   │ 名字         │ 题数    │ 说明                          │
   ├──────────────┼────────┼──────────────────────────────┤
   │ SWE-bench    │ 2,294  │ ✅ 全量                       │
   │ Lite         │   300  │ ✅ 论文给出的精简子集         │
   │ Verified     │   500  │ ⚠️ 人工核验过题目可解的子集   │
   └──────────────┴────────┴──────────────────────────────┘

   ⚠️ 🔑 ★★★ 三个数【绝对不能横向比较】。Verified 上的分数
      系统性高于全量，因为它剔除了描述不清、根本做不了的题。
```

---

## E. ✅ 值得记住的关键数字

★★ 全部有论文原文出处，章节号在最后一列。

| 数字 | 含义 | 章 |
|---|---|---|
| **2,294** | SWE-bench 全量题数（12 个仓库） | 07 |
| **90,000 → 2,294** | SWE-bench 三级漏斗的筛选比例 | 07 |
| **1.96%** | Claude 2 在 SWE-bench 上的原始成绩（2023） | 07 |
| **2.67 / 7.33 / 11.00 / 18.00** | ★★★ 同一个 GPT-4 Turbo，四种脚手架，SWE-bench Lite | 08 |
| **8-13×** | SWE-agent 相比 RAG 的成本倍数（换来 6.7 倍分数） | 08 |
| **18.0 → 10.3** | 拿掉 edit 命令，掉 7.7 个点 | 08 |
| **15.7 vs 12.0** | ★★★ 没有搜索 > 设计得差的搜索 | 08 |
| **51.7%** | SWE-agent 运行轨迹中至少写错过一次编辑的比例 | 08 |
| **>80%** | LLM 裁判和人类的一致率（MT-Bench） | 09 |
| **65.0% / 30.0%** | GPT-4 作裁判的位置偏差（一致率 / 偏第一个） | 09 |
| **91.3 / 8.7** | 冗长偏差：裁判被更长回答骗到的比例 | 09 |
| **14/20 → 6/20 → 3/20** | 数学题上，裸判 / CoT / 带参考答案的失败数 | 09 |
| **+10% / +25%** | GPT-4 / Claude-v1 的自我偏好偏差幅度 | 09 |
| **0.3** | ✅ DeepSeek 的噪声下限：差距 ≤ 0.3 算同一水平 | 10 |
| **0 ~ 25** | ✅ DeepSeek-V3 各基准的 few-shot 数范围 | 10 |
| **8,192 / 32,768** | ✅ V3 / R1 的最大输出 token 数 | 10 |
| **0.6 / 0.95** | ✅ R1 的 temperature / top-p | 10 |
| **42.0 / 50.8 / 38.8** | ✅ SWE-bench Verified：V3 / Claude-3.5-Sonnet / GPT-4o | 10 |
| **600 万** | ✅ R1 仅数学领域就过滤掉的疑似污染预训练文本条数 | 10 |
| **~60 → ~0** | ★★★ ✅ DS-Base-33B 在 LeetCode 上跨越训练截止日的断崖 | 06 |
| **59.8 vs 26.3** | ✅ DS-Ins-1.3B：HumanEval+ vs LiveCodeBench-Easy | 06 |
| **511** | ✅ LiveCodeBench 的题量（2023-05 ~ 2024-05） | 06 |

---

## F. 排查清单：分数明显不对时按这个顺序查

```
   ⚠️ 顺序是按【出错概率从高到低】排的，不要跳着查。

   1. ★★★ 对话模板（chat template）用对了吗？
      ⟹ 04 章。chat 模型套了 base 模型的拼法 = 分数腰斩。
      ⟹ lm_eval 里对应 --apply_chat_template

   2. ★★★ 抽取规则匹配得上模型的输出格式吗？
      ⟹ 05 章。看几条原始输出（--log_samples），
         对比 resps 和 filtered_resps。
      ⟹ ⚠️ 大量 "[invalid]" = 抽取挂了，不是模型笨。

   3. ★★ few-shot 数量和别人一致吗？示例是怎么选的？
      ⟹ 04 章 + 10.4。

   4. ★★ 停止符（until）设对了吗？
      ⟹ 设错会把答案截断，或者让模型继续编下一道题。

   5. ★★ 温度 / 采样次数和对比对象一致吗？
      ⟹ 10.7b。

   6. ★ 数据集版本 / 子集选对了吗？
      ⟹ 例：SWE-bench 全量还是 Verified。

   7. ★ 模型 checkpoint 或 API 版本变了吗？
      ⟹ 闭源 API 会静默升级。
```

---

## G. 常用命令（lm-evaluation-harness）

```bash
# 冒烟测试：只跑 20 条，看流程通不通
lm_eval --model hf --model_args pretrained=<model> \
        --tasks gsm8k --limit 20 --log_samples --output_path ./out

# ★★★ chat 模型必须加这个
lm_eval --model hf --model_args pretrained=<model> \
        --tasks mmlu --apply_chat_template

# 改 shot 数
lm_eval ... --tasks mmlu --num_fewshot 5

# ★★ 加载你自己写的任务 yaml
lm_eval ... --include_path ./my_tasks --tasks my_task
```

```
   ⟹ 🔑 三个必用参数：
   ★★★ --log_samples   保存每条的原始输出，★ 出问题时唯一的线索
   ★★  --limit         先跑 20 条，别一上来就烧钱
   ★★★ --apply_chat_template   chat 模型的默认动作
```

---

> 返回 [评测入门总目录](README.md)
