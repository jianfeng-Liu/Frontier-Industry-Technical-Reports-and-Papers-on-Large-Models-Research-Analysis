> [← 13 业界横向对照](13-业界横向对照.md) ｜ [目录](README.md) ｜ [附 速查表](附-速查表.md)

# 14 · ⭐⭐ 演进主线与开放问题

> **这一章是全教程的脊柱，可以单独读。**
> 前半段把 2021→2026 串成一条因果链；后半段列出还没有答案的问题。
> **标记**：✅ 有出处的结论 / ⚠️ 我的判断 / 🔮 开放问题。

---

## 14.1 ★★ 三代范式，一条因果链

### 第一代（2021~2023）：**对齐时代** —— "把图变成一句话"

```
    要解决的问题：LLM 根本不认识图
    核心动作：    CLIP 把图和文拉进同一个空间；连接器把视觉向量翻译给 LLM
    典型形态：    冻住的视觉塔 + 一个 MLP + 冻住的 LLM，60 万条指令数据
    ★ 心智模型：  一张图 ≈ 一句话的信息量

    ⟹ 留下的问题：★★ 224×224 的输入，模型【看不清任何细节】
                  一张图被压成 576 个 token，文档、表格、小字全糊
```

### 第二代（2023~2025）：**看清时代** —— "把 token 预算花对地方"

```
    要解决的问题：看不清
    核心动作：    动态分辨率、AnyRes 切图、2×2 合并、2D/3D RoPE、
                 recaption 重写数据、原生多模态预训练
    ★ 心智模型：  一张图 = 几千个 token，是一份【要精打细算的预算】

    ⟹ ★ 这一代的成果是压倒性的：
       模型第一次真的能读 PDF、读发票、读代码截图
    ⟹ 留下的问题：① 幻觉没解决（看清了照样编）
                  ② 尺子没有（各家 benchmark 零重叠）
                  ③ ★★ 更根本的：看清了【然后呢】？
                     "描述这张图"本身不是一个有多大商业价值的任务
```

### 第三代（2025~2026）：**使用时代** —— "视觉是 agent 的眼睛和验尺"

```
    要解决的问题：看清之后干什么
    核心动作：    · GUI agent：看屏幕 → 决定点哪 → 看结果
                 · ✅ 渲染回环：写代码 → 渲染 → 【自己看】→ 改
                 · agentic 视觉：主动裁剪放大，"再看一眼"
                 · 多模态 RL：奖励来自环境反馈，而不是人类偏好
    ★★ 心智模型： 视觉不是【输入模态】，是【传感器 + 验证器】

    ✅ 一手佐证（GLM-5.3）：
       "Vision therefore needs to be natively integrated into the model,
        enabling it to decide when to observe and use visual feedback
        to guide subsequent actions."
```

### ★★ 把三代连成因果链

```
     LLM 不认识图
        │
        ├─→ 【第一代】用 CLIP 对齐，图 ≈ 一句话
        │       │
        │       └─→ 副作用：分辨率被 CLIP 的 224 卡死 ⟹ 看不清
        │                       │
        ├───────────────────────┴─→ 【第二代】动态分辨率 + token 预算管理
        │                              │
        │                              ├─→ 成果：能读文档了
        │                              └─→ 副作用：token 暴涨 ⟹ EPD 分离、
        │                                          KV 爆炸、评测口径失控
        │                                              │
        └──────────────────────────────────────────────┴─→ 【第三代】
                    "看" 变成 agent 循环里的一个【动作】，
                    而不是请求开始时的一次性输入
                    ⟹ 视觉的价值从"理解世界"变成"验证自己"
```

> 🔑 **如果这份教程只留一句话**：
> **多模态的重心，正在从"让模型看懂图"转移到"让模型用眼睛检查自己干的活"。**
> ⚠️ 这是我的判断，但它有一条硬证据：**GLM-5.3 是五家里唯一给出多模态存在理由的，
> 而它给的理由正是这一条。**

---

## 14.2 ⚠️ 顺带解释三个"为什么"

```
 ⚠️ 为什么 Q-Former 死了？（03 章）
    因为第一代的心智模型是"图 ≈ 一句话"⟹ 压到 32 个 token 完全合理。
    第二代的心智模型变成"图 = 一份预算"⟹ 硬压缩就成了不可接受的信息损失。
    ⟹ ★ 不是它技术差，是【前提变了】。

 ⚠️ 为什么 2026 年还有旗舰模型是纯文本？（13 章）
    因为前两代的产出是"看得清"，而看得清对代码和数学推理【没有直接帮助】。
    ⟹ 第三代的"视觉当验证器"才第一次给了推理型模型做视觉的理由
      —— 而这个理由 2026 年才刚刚被论证清楚。

 ⚠️ 为什么"统一理解与生成"雷声大雨点小？（09 章）
    因为它解决的是【交互形态】问题，不是【能力】问题，
    而三代主线一直在追能力。
    ⟹ ★ 它可能要等到"生成"本身成为 agent 回路的一部分才真正起飞
      （比如：模型画一张设计草图 → 自己看 → 改）。
```

---

## 14.3 🔮 八个开放问题

### 🔮 ① 多模态到底能不能提升纯文本智能？

```
    ⟹ 至今【没有公开的对照实验】：同样的算力，一次纯文本、一次混多模态，比最终推理分数。
    ⟹ 因为做一次要花两次预训练的钱。
    ⚠️ 间接证据反而是负面的：最强的推理模型（V4）是纯文本的。
    ★ 这是本领域最贵、也最重要的一个未解问题。
```

### 🔮 ② 还需要独立的视觉编码器吗？

```
    ✅ K3 已经证明"对比学习初始化不是必需的"。
    ⚠️ 再往前一步就是 Fuyu 那条"干脆不要编码器"的路（02.6 节）。
    🔮 如果规模继续涨，视觉塔会不会像 tokenizer 一样，
       退化成一个几乎无参数的预处理步骤？
```

### 🔮 ③ patch 是最好的视觉表示吗？

```
    ⚠️ "把图切成方格"是从 2020 年 ViT 沿用至今的，没有人认真挑战过。
    它的问题很明显：★ 均匀切分意味着【天空和人脸花一样多的 token】。
    🔮 有没有一种"按信息量分配 token"的表示？
       （区域提议、可变形 patch、目标为中心的表示……都试过，都没成为主流）
```

### 🔮 ④ 幻觉能被根治吗？

```
    ⚠️ 8.2 节的结论是：幻觉源于"语言先验 vs 视觉证据"的冲突。
    只要主干 99% 的参数来自文本，这个不对称就存在。
    🔮 grounding（强制输出坐标）是我看好的方向，因为它让错误【可验证】。
    ⟹ 但"可验证"到"不犯错"之间还隔着整个 RL 的路要走。
```

### 🔮 ⑤ 多模态的尺子什么时候会有？

```
    ✅ 现状（11、13 章）：K3 和 M3 零个共享 benchmark。
    ⚠️ 我不认为这会自然好转 —— 因为 benchmark 太多，
       每家都能找到自己赢的那几个，【没有人有动力统一】。
    🔮 可能的转机：像 SWE-bench 之于代码那样，
       出现一个【任务真实、判分客观、没法刷】的多模态 agent 基准。
       ⟹ GUI 任务成功率是最有希望的候选，因为它的判分是环境给的。
```

### 🔮 ⑥ 长视频的正确形态是什么？

```
    ⚠️ 三条路（硬塞 / 摘要 / 检索）都不干净（05.5 节）。
    🔮 我倾向检索路线会赢，因为它和文本 RAG 同构，
       而文本领域已经证明了"长上下文"和"检索"是互补而不是替代关系。
```

### 🔮 ⑦ 实时和智能的对立怎么破？

```
    ⚠️ 10.5 节：端到端语音模型不能做长思维链，因为用户不会等 30 秒。
    🔮 可能的解法是【分层】：一个快模型维持对话节奏，
       一个慢模型在后台想 —— 这和具身智能的"双系统架构"是同一个思路
       （见 ../具身智能与世界模型/01-具身智能与VLA.md 1.4 节）。
    ⟹ ★ 不同领域撞到同一堵墙、长出同一个解法，通常说明这个解法是对的。
```

### 🔮 ⑧ 视觉预训练数据会不会耗尽？

```
    ⚠️ 文本数据的枯竭已经在讨论（见 ../数据格式和数据处理/11-喂多少-ScalingLaw.md）。
    图像看起来无穷无尽，但【高质量、有信息的图文配对】并不是。
    🔮 而多模态有一张纯文本没有的牌：★ 渲染合成（07.4 节）——
       能被程序画出来的东西可以无限量地完美标注。
    ⟹ 这可能让多模态的数据墙比文本来得晚。⚠️ 但它只覆盖"合成得出来"的那部分世界。
```

---

## 14.4 🔑 全教程的五句话总结

1. **多模态的全部难点，是在没有词典的情况下把图/音变成"像文字一样的向量"**，然后管理好它带来的 token 账。
2. **三代范式：对齐（图≈一句话）→ 看清（图=一份预算）→ 使用（视觉=agent 的传感器与验证器）。** 每一代的副作用都是下一代的起点。
3. ✅ **2026 年的架构已经收敛**（patch 14 + 2×2 合并 + 中等 ViT），**差距在数据和训练**；而**五家里仍有两家不做多模态**，且都是推理导向。
4. ⚠️ **两件事仍然没解决：幻觉（源于语言先验压过视觉证据）和尺子（各家 benchmark 零重叠）。**
5. 🔮 **最重要的开放问题是"多模态能不能提升纯文本智能"**——至今没有公开的对照实验，而间接证据偏负面。

---

> **回到** → [目录](README.md) ｜ **随时查缩写** → [附 速查表](附-速查表.md)
