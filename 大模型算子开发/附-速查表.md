# 附 · 速查表：硬件参数、关键公式、工具链对照

> 本附录汇总各章反复用到的数字和公式，不展开推导，随用随翻。  
> 数字来源：NVIDIA H100 SXM5 白皮书（2023）、各章实测结果。

---

## A. H100 SXM5 硬件参数

| 参数 | 数值 | 备注 |
|---|---|---|
| SM 数量 | 132 | Streaming Multiprocessor |
| 每 SM CUDA Core (FP32) | 128 | — |
| 每 SM Tensor Core | 4 | Hopper 第四代 |
| 每 SM 最大 Warp 数 | 64 | = 2048 线程/SM |
| 每 SM 寄存器 | 65536 × 32-bit | = 256 KB/SM |
| 每 SM 共享内存 (最大) | 228 KB | 可配置 SRAM/L1 比例 |
| L2 缓存 | 50 MB | 全芯片共享 |
| HBM3 总容量 | 80 GB | SXM5 版本 |
| HBM3 带宽 | ~3.35 TB/s | = 3350 GB/s |
| FP32 峰值算力 | ~67 TFLOPS | CUDA Core |
| TF32 峰值算力 | ~494 TFLOPS | Tensor Core，稀疏 2× |
| BF16/FP16 峰值算力 | ~989 TFLOPS | Tensor Core，稠密 |
| FP8 峰值算力 | ~1979 TFLOPS | Tensor Core，稠密 |
| INT8 峰值算力 | ~1979 TOPS | Tensor Core，稠密 |
| NVLink 带宽 (双向) | 900 GB/s | 单卡对外，8 卡 NVSwitch |
| TDP | 700 W | SXM5 |

> ✅ 所有 Tensor Core 操作要求矩阵维度是 **16 的倍数**。

---

## B. 关键公式速查

### B.1 算术强度与屋顶线

```
算术强度 I = FLOPs / Bytes（读写总字节数）

屋顶线性能上界：
  P = min(峰值算力, 峰值带宽 × I)
  H100 屋脊点 = 989 TFLOPS / 3350 GB/s ≈ 295 FLOP/Byte

  I < 295：Memory Bound，P ≈ 3350 × I (GFLOPS，I 单位 FLOP/Byte)
  I ≥ 295：Compute Bound，P → 989 TFLOPS
```

### B.2 GEMM 算术强度

```
A: M×K，B: K×N，C: M×N（FP16，2 Bytes/元素）

FLOPs  = 2·M·K·N
Bytes  = 2·(M·K + K·N + M·N)

I ≈ M·K·N / (M·K + K·N + M·N)

特殊情形：
  M=1（推理 batch=1）：I ≈ 1 FLOP/Byte
  M=64：              I ≈ 64 FLOP/Byte（近似，K=N 时）
  M=N=K=大数：        I ≈ M/2 FLOP/Byte → Compute Bound
```

### B.3 MFU（模型算力利用率）

```
MFU = (tokens/s × 6N) / 峰值 TFLOPS

N = 参数量（6N ≈ 每 token 的 FLOPs，包含前向 + 反向 × 2 + 优化器）
业界顶级：MFU ≈ 40–50%（单机 8×H100）
```

### B.4 注意力内存开销

```
朴素 Attention 中间矩阵 S = QK^T：
  大小 = B × H × N × N × 2 Bytes（FP16）
  N=8192, H=32, B=1：≈ 4 GB
  N=32768：≈ 64 GB（超出 H100 80 GB 显存！）

FlashAttention 重计算后：
  保存量 = O(N)，与序列长度线性
```

### B.5 KV Cache 大小

```
每 token 每层 KV Cache（FP16）：
  = 2 × 2 × d_model × sizeof(FP16)
  = 2 × 2 × d_model × 2 Bytes
  = 8 × d_model Bytes

LLaMA-3 70B 示例（d_model=8192，80 层）：
  每 token = 8 × 8192 = 65536 Bytes ≈ 64 KB（单层）× 80 层 = 5 MB/token
  注：GQA 时按实际 KV head 数折算
```

### B.6 量化误差与位宽

```
对称量化（scale-only）：
  x_q = round(x / s)，x_dq = x_q × s
  s = max(|x|) / (2^(b-1) - 1)

FP8 E4M3：动态范围 ±448，精度 ~3 有效位（十进制）
FP8 E5M2：动态范围 ±57344，精度 ~2 有效位（十进制）
INT8：   动态范围 [-128, 127]，精度 ~2.4 有效位
INT4：   动态范围 [-8, 7]，精度 ~1.2 有效位
```

---

## C. 常见算子算术强度速查

| 算子 | 典型 I (FLOP/Byte) | 瓶颈 | 首选优化 |
|---|---|---|---|
| GEMM，batch=1（推理） | 1–10 | Memory | 量化 / 增大 batch |
| GEMM，batch≥32（训练） | 64–512 | Memory→Compute | 三级分块 / cuBLAS |
| GEMM，大方阵 | 500+ | Compute | Tensor Core 调优 |
| FlashAttention（fused） | 60–200 | Memory→Compute | FA2/FA3 已最优 |
| 朴素 Softmax | 2–5 | Memory | 融合或换 FA |
| LayerNorm / RMSNorm | 2–8 | Memory | 融合进 GEMM Epilogue |
| 激活函数（SiLU/GeLU） | 1–3 | Memory | 必须融合 |
| Decode GEMM（M=1） | ~1 | Memory | 投机采样 / 量化 |

---

## D. FlashAttention 版本对比

| 特性 | FA1 | FA2 | FA3 |
|---|---|---|---|
| 年份 | 2022 | 2023 | 2024 |
| 核心改进 | KV 分块 + 在线 Softmax | 减少非 MMA 操作，GQA | wgmma + TMA，流水线 |
| H100 BF16 吞吐 | — | ~540 TFLOPS | ~810 TFLOPS |
| FP8 支持 | ✗ | ✗ | ✓ |
| GQA/MQA 原生支持 | ✗ | ✓ | ✓ |
| varlen 支持 | ✓ | ✓ | ✓ |
| PyTorch 集成 | 手动 | `F.sdpa` 自动调用 | `flash-attn≥2.6` |

---

## E. 量化方案对比

| 方案 | 精度 | 需要校准数据 | 速度提升 | 精度损失 | 适用场景 |
|---|---|---|---|---|---|
| W16A16（基线） | FP16 | ✗ | 1× | 无 | 训练 / 高精度推理 |
| W8A16 | INT8 权重，FP16 激活 | ✗ | ~1.5× | 极小 | 通用推理 |
| W8A8（SmoothQuant） | INT8/INT8 | ✓（小量） | ~2× | 小 | 吞吐敏感推理 |
| W4A16（GPTQ/AWQ） | INT4 权重，FP16 激活 | ✓ | ~2–3× | 小~中 | 显存受限 |
| FP8 E4M3/E5M2 | FP8 | ✓ | ~2× | 小 | H100 训练/推理 |

---

## F. 工具链速查

| 工具 | 类别 | 核心用途 | 可定制性 |
|---|---|---|---|
| cuBLAS | 运行时库 | 极速 GEMM，开箱即用 | 无，不可定制 Epilogue |
| CUTLASS 3.x | 模板库 | 可定制 Epilogue 的 GEMM | 高，C++ 模板 |
| Triton | DSL | Python 写 GPU kernel | 中，Block 粒度 |
| FlashAttention-2/3 | 手写 kernel | 最优 Attention | 低，用即用 |
| cuDNN | 运行时库 | Conv、LSTM、LayerNorm | 无 |
| Nsight Systems | Profiler | 全局时间线，定位慢 kernel | — |
| Nsight Compute | Profiler | 单 kernel 深度分析 | — |
| torch.compile | 编译器 | Python 模型 → 融合 kernel | 中（后端可选） |
| TorchInductor | 后端 | 为 Triton 生成代码 | — |
| vLLM | 推理框架 | PagedAttention，连续批处理 | — |
| SGLang | 推理框架 | RadixAttention，结构化生成 | — |
| TRT-LLM | 推理框架 | TensorRT 后端，量化融合 | — |

---

## G. MoE 关键数字（DeepSeek-V3 规模参考）

```
模型规模：671B 参数，37B 激活
专家配置：256 专家，Top-8 路由，EP=32（每节点 8 卡）
隐藏维度：H = 7168

All-to-All 通信量（每层，单向）：
  = tokens × H × sizeof(BF16) × (EP-1)/EP
  例：batch=4096 tokens，H=7168，EP=32
  ≈ 4096 × 7168 × 2 × 31/32 ≈ 1.76 GB（发送）

Grouped GEMM 的不规则性：
  Top-K=8，每专家平均 tokens = batch × 8 / 256 = batch/32
  batch=4096 → 每专家约 128 tokens（实际按 Poisson 分布，有专家空 / 过载）
```

---

## H. 推理侧关键概念速查

| 概念 | 一句话定义 | 章节 |
|---|---|---|
| KV Cache | 保存已计算的 K、V，Decode 时不重算 | 09 |
| PagedAttention | KV Cache 按物理 Page 管理，逻辑连续物理不连续 | 09 |
| 连续批处理 | 迭代级调度，不同序列异步结束/加入 | 09 |
| 投机采样 | 小草稿模型生成多步，大模型并行验证 | 09 |
| RadixAttention | Radix Tree 前缀复用 KV Cache | 09 |
| Chunked Prefill | Prefill 切片与 Decode 交错，降首 token 延迟 | 09 |
| Prefill | 首次处理全部 prompt，计算密集 (Compute Bound) | 09 |
| Decode | 逐 token 自回归生成，带宽密集 (Memory Bound) | 09 |

---

## I. 常见维度对齐要求

```
Tensor Core 对齐（BF16/FP16）：M, N, K 均须是 16 的倍数
高效 GEMM 对齐：通常要求 128 的倍数（8 × 16）
INT8 Tensor Core：M, N, K 须是 16 的倍数
FP8 Tensor Core：M, N, K 须是 16 的倍数
共享内存 bank：32 个 bank，每 bank 4 Bytes（32-bit）
Warp 大小：固定 32 线程，Block 大小应是 32 的倍数
```

---

**本速查表对应章节**：

- 硬件参数 → [01 章](01-GPU硬件基础.md)  
- 屋顶线公式 → [02 章](02-屋顶线与性能剖析.md)  
- GEMM 算术强度 → [03 章](03-GEMM深度剖析.md)  
- FlashAttention → [04 章](04-FlashAttention全系.md)  
- 量化方案 → [06 章](06-量化算子.md)  
- 工具链 → [07 章](07-Triton编程.md)  
- MoE 数字 → [08 章](08-MoE算子与通信.md)  
- 推理概念 → [09 章](09-推理侧算子.md)
