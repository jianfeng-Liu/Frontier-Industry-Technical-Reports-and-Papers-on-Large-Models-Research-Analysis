> [目录](README.md) ｜ 下一章 → [02 数据源与文本抽取](02-数据源与文本抽取.md)

# 01 · 鸟瞰：一条数据从网页到 GPU 的完整旅程

> **这一章不讲任何细节，只把整条流水线的形状看一遍。** 后面每一章都是在放大其中一个方框。

---

## 1.1 先说一个数字，建立方位感

训练一个前沿大模型，要喂 **15 万亿（15 T）个 token**。

这是什么概念？

```
一本《三体》全集         ≈  1.5 百万字   ≈ 1.5 M token
15 T token              ≈  1000 万套《三体》全集
                        ≈ 一个人不吃不喝不睡、每秒读 10 个字，读 5 万年
```

这些字**不是谁写出来的，是从互联网上抓下来的**。而互联网上的文字，绝大部分是垃圾：导航栏、版权声明、"点击这里查看更多"、同一篇新闻被 500 个网站转载、自动生成的商品页面……

> ★★ **整个数据处理领域，本质上只在回答一个问题：**
> **从 100 TB 的原始网页里，怎么挑出 15 TB 值得让模型学的东西？**

而且这个挑选过程必须是**自动的**——没有任何团队能雇人读 100 TB。所以领域里的每一篇论文，都是在提出一种"自动判断这段文字值不值得学"的办法。

---

## 1.2 全景图：九个环节

```
                    ┌──────────────────────────────────────┐
                    │  ①  数据源                            │
                    │     Common Crawl 每月一次全网快照      │
                    │     + 代码仓库 + 书 + 论文 + 百科      │
                    │     体量：~ 100 TB 压缩后的原始网页    │
                    └──────────────┬───────────────────────┘
                                   │  【02 章】
                    ┌──────────────▼───────────────────────┐
                    │  ②  文本抽取                          │
                    │     从 HTML 里把正文抠出来             │
                    │     扔掉导航、广告、页脚、脚本          │
                    │     ★ 这一步的质量，上限了后面一切     │
                    └──────────────┬───────────────────────┘
                                   │  【05 章】
                    ┌──────────────▼───────────────────────┐
                    │  ③  语言识别 + 启发式过滤              │
                    │     "是不是中/英文" "行数够不够"       │
                    │     "有没有 20 个连续的省略号"         │
                    │     ★ 最便宜的一刀，砍掉大半           │
                    └──────────────┬───────────────────────┘
                                   │  【06 章】
                    ┌──────────────▼───────────────────────┐
                    │  ④  去重  ★★★                        │
                    │     完全一样的 / 差不多一样的 /        │
                    │     有一大段一样的 / 意思一样的        │
                    │     ★ 收益最确凿的一步                 │
                    └──────────────┬───────────────────────┘
                                   │  【07 章】
                    ┌──────────────▼───────────────────────┐
                    │  ⑤  质量过滤                          │
                    │     给每篇文档打一个"值不值得学"的分    │
                    │     困惑度 → fastText → 教育分         │
                    └──────────────┬───────────────────────┘
                                   │  【08 章】
                    ┌──────────────▼───────────────────────┐
                    │  ⑥  去污染 + 隐私 + 许可               │
                    │     把评测题目从训练集里删掉           │
                    │     把手机号身份证号抹掉               │
                    └──────────────┬───────────────────────┘
                                   │  【10、12 章】
                    ┌──────────────▼───────────────────────┐
                    │  ⑦  配比 + 合成                       │
                    │     网页 70% + 代码 15% + 数学 5% ...  │
                    │     不够的部分用模型自己造             │
                    │     产物：~ 15 TB 的干净文本           │
                    └──────────────┬───────────────────────┘
                                   │  【09 章】
                    ┌──────────────▼───────────────────────┐
                    │  ⑧  Tokenize + 落成训练格式           │
                    │     文本 → 整数 id → .bin / .idx      │
                    │     ★ 从这一步起，就再也不是"文本"了   │
                    └──────────────┬───────────────────────┘
                                   │  【04、13、14 章】
                    ┌──────────────▼───────────────────────┐
                    │  ⑨  DataLoader：采样 + 打包 + shuffle │
                    │     拼成定长 [B, L] 的整数张量         │
                    │     ★ 每个 step 现算，不落盘           │
                    └──────────────┬───────────────────────┘
                                   ▼
                             ★ GPU 上的 tensor
                            [B, L] int32/int64
```

---

## 1.3 ★ 一个必须先建立的分界线：离线 vs 在线

初学最容易搞混的一件事：**上面九个环节不是同时发生的。** 它们被一条线劈成两半：

```
┌─── 离线（offline）：训练开始【之前】跑，跑一次，产物落盘 ──────┐
│                                                              │
│   ① 数据源 → ② 抽取 → ③ 过滤 → ④ 去重 → ⑤ 质量分            │
│   → ⑥ 去污染 → ⑦ 配比合成 → ⑧ tokenize 落盘                 │
│                                                              │
│   耗时：几天到几周，几千台 CPU 机器                            │
│   产出：一堆 .bin / .idx 文件，几十 TB                        │
│   ★ 跑完就不动了。同一份数据可以给很多次训练复用               │
└──────────────────────────────────────────────────────────────┘
                            │
                            │  数据交接点：.bin / .idx
                            ▼
┌─── 在线（online）：训练【过程中】每个 step 都在跑 ────────────┐
│                                                              │
│   ⑨ 按配比采样 → 拼接/打包 → shuffle → 组 batch              │
│                                                              │
│   耗时：每个 step 几毫秒，在 CPU 上和 GPU 计算重叠            │
│   产出：不落盘，直接是内存里的 tensor                          │
│   ★ 必须严格可复现：崩了重启要能接着喂同样的数据               │
└──────────────────────────────────────────────────────────────┘
```

> ★★ **这条线的位置就是 `.bin` / `.idx` 文件。** 它是数据团队和训练团队的交接界面。
> 你听到有人说"数据准备好了"，通常指的就是"离线那一半跑完了，`.bin/.idx` 已经在共享存储上了"。

**为什么要这么切？** 因为离线那半非常贵（几千台机器跑几周），而训练要跑很多次（调超参、改架构、重启）。**把贵且不变的部分算一次存起来，是最基本的工程决策。**

---

## 1.4 每一步砍掉多少：一个真实的数量级

这是初学者最缺的直觉——**到底砍得有多狠**。

以 **FineWeb**（HuggingFace 的开源语料，[arXiv:2406.17557](https://arxiv.org/abs/2406.17557)，2024-06-25）的公开流程为例，⚠️ **下面的百分比是我按各环节公开描述整理的量级示意，不是论文里的一张官方表格**：

```
Common Crawl 单次快照（压缩 WARC）      ~ 90 TB
        │
        │  ② 抽取正文（HTML → text）
        ▼                                 ~ 剩 15%   ← 网页里 85% 的字节是标签和脚本
        │
        │  ③ 语言识别（只留英文）+ 启发式规则
        ▼                                 ~ 剩 30%
        │
        │  ④ 去重（MinHash 近似去重）
        ▼                                 ~ 剩 10~30%  ★ 这一刀最狠
        │
        │  ⑤ 质量过滤
        ▼                                 ~ 剩 50~70%
        │
   最终留下                              总体约 1~5% 的原始字节
```

> ★ **"丢掉 95% 以上"是这个领域的常态，不是做错了。**
> 更极端的例子：**FineWeb-Edu**（FineWeb 的教育子集）只保留了 FineWeb 15 T token 里的 **1.3 T**，也就是**再砍掉 91%**——而砍完之后，在 MMLU、ARC 这些知识类评测上**反而大幅变好**。✅（[arXiv:2406.17557](https://arxiv.org/abs/2406.17557)）
>
> ★ 顺手把这两个名字解释掉（后面会反复出现）：
> **MMLU** = **M**assive **M**ultitask **L**anguage **U**nderstanding（大规模多任务语言理解），57 个学科的选择题，是"知识量"的标准考卷；
> **ARC** = **A**I2 **R**easoning **C**hallenge（AI2 推理挑战），小学科学题，考的是常识推理。
> ⚠️ 注意：[16 章](16-五家报告的数据章节.md)里 openPangu 报告用 **ARC** 指的是**另一个意思**（Agent / Reasoning / Code 三类数据），同名不同义，别搞混。

这引出这个领域最反直觉、也最重要的一条经验：

> ★★★ **数据不是越多越好。在同样的训练预算下，把差数据删掉，比留着它训练更好。**
> 因为训练预算（喂多少 token）是固定的——**每一个垃圾 token 占的位置，本来可以放一个好 token。**

---

## 1.5 ★ 三条贯穿全书的因果链

和隔壁架构教程一样，这个领域也有几条"一通百通"的主线。先放在这，后面每章会回来引用：

```
① 训练预算固定（喂 15 T token 就是 15 T）
     → 每个位置只能放一个 token
     → 放垃圾 = 挤掉一个好的
     → ★ 过滤和去重的收益，本质是【机会成本】，不是"省钱"
   ⟹ 解释了：为什么砍掉 95% 反而更好、为什么 FineWeb-Edu 只留 1.3 T

② 重复的文本会被模型【重复学习】
     → 同一句话见 1000 遍 → 模型倾向于原样背下来（记忆而非泛化）
     → ① 浪费预算 ② 原文泄露风险 ③ 评测集若混进训练集 → 分数虚高
   ⟹ 解释了：去重（06 章）、去污染（08 章）、以及为什么它俩总被放在一起讲

③ 网页的总量是有限的，而模型胃口在涨
     → 高质量网页快不够用了
     → 出路只有三条：a) 重复用（11 章）
                      b) 造新的（12 章）
                      c) 把过滤放松、靠配比补救（10 章）
   ⟹ 解释了：2024 年后这个领域的全部新论文都在这三个方向上
```

---

## 1.6 一条数据的具体样子：一路跟到底

抽象讲完了，来看**同一条数据**在九个环节里长什么样。

### ① 数据源：一条 WARC 记录

**WARC**（Web ARChive，网页归档格式）是 Common Crawl 存网页的标准格式。它存的是**原始 HTTP 响应**：

```
WARC/1.0
WARC-Type: response
WARC-Target-URI: http://example.com/recipe/tomato-egg
WARC-Date: 2024-03-01T12:00:00Z
Content-Length: 8231

HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!DOCTYPE html><html><head><script src="ga.js"></script>
<title>番茄炒蛋的做法 - 美食网</title></head>
<body><div class="nav">首页|菜谱|登录</div>
<div class="content"><h1>番茄炒蛋</h1>
<p>番茄炒蛋是一道家常菜。先把鸡蛋打散……</p></div>
<div class="footer">版权所有 © 2024 美食网 | 备案号...</div>
</body></html>
```

### ② 抽取后：只剩正文

```
番茄炒蛋

番茄炒蛋是一道家常菜。先把鸡蛋打散……
```

★ 注意 `首页|菜谱|登录`、`版权所有 ©...`、`<script>` **全都没了**。这就是抽取器（如 `trafilatura`）干的活。

### ③~⑥ 过滤、去重、质量分之后：变成一条 JSON

```json
{
  "id": "cc-2024-10-0000123456",
  "text": "番茄炒蛋\n\n番茄炒蛋是一道家常菜。先把鸡蛋打散……",
  "url": "http://example.com/recipe/tomato-egg",
  "language": "zh",
  "language_score": 0.98,
  "quality_score": 2.7,
  "token_count": 312,
  "dump": "CC-MAIN-2024-10"
}
```

★ 这时它还是**人能读的文本**。这一步的格式叫 **JSONL**（03 章）。

### ⑧ Tokenize 之后：变成整数

```
"番茄炒蛋是一道家常菜" → [45231, 8891, 15, 3204, 992, 6710, 128]
                          ↑ 每个整数是词表里的一个下标
```

★ **从这一步开始，数据就不是文本了，是一串 int32。** 落盘成 `.bin`（04 章）。

### ⑨ DataLoader 拼出来的最终产物

```
一条训练样本 = 长度恰好 4096 的整数数组

[45231, 8891, 15, ..., <eod>, 7712, 331, ..., <eod>, 88, 21, ...]
 └──── 文档 A（312 token）───┘  └── 文档 B（1804 token）──┘  └ 文档 C 的前半截 ┘
                             ↑                          ↑
                     文档结束符 <eod>            ★ 一条样本里【混了好几篇文档】
                                                   这就是 13 章要讲的 packing

一个 batch = [B, 4096] 的 int 张量  →  丢给 GPU
```

> ★★ 看到这里你应该注意到一件事：**一条训练样本里通常混着好几篇毫不相干的文档**（一段菜谱 + 一段法律条文 + 半段代码）。这是刻意的设计，理由和随之而来的麻烦见 [13 章](13-打包与文档掩码.md)。

---

## 1.7 Python 伪代码：把整条流水线写成 20 行

真实系统当然不长这样（每一步都是几千台机器的分布式任务），但**逻辑骨架**就是这个：

```python
# ============ 离线部分：跑一次，产物落盘 ============
docs = []
for warc_file in common_crawl_shards:          # ①
    for record in read_warc(warc_file):
        text = extract_main_text(record.html)  # ② 抽正文
        if not text:
            continue
        if detect_language(text) != "zh":      # ③ 语言识别
            continue
        if not heuristic_ok(text):             # ③ 启发式规则
            continue
        docs.append({"text": text, "url": record.url})

docs = dedup_exact(docs)                       # ④ 精确去重
docs = dedup_minhash(docs, threshold=0.8)      # ④ 近似去重
docs = [d for d in docs if quality(d) > 2.0]   # ⑤ 质量分
docs = remove_benchmark_overlap(docs)          # ⑥ 去污染

mixed = blend({"web": docs, "code": code_docs, # ⑦ 配比
               "math": math_docs},
              weights=[0.70, 0.20, 0.10])

with IndexedDatasetBuilder("train") as builder: # ⑧ tokenize + 落盘
    for d in mixed:
        ids = tokenizer.encode(d["text"]) + [EOD]
        builder.add_document(ids)
# → 产出 train.bin + train.idx

# ============ 在线部分：训练时每个 step 都在跑 ============
for step in range(total_steps):                 # ⑨
    batch = dataloader.next()   # 从 .bin 里按索引取 → 拼成 [B, L]
    loss = model(batch)
    loss.backward()
```

---

## 1.8 挂回真实系统

| 环节 | 现实中你会听到的名字 | 讲在哪章 |
|---|---|---|
| ① 数据源 | Common Crawl、GitHub、arXiv、Books3 | [02](02-数据源与文本抽取.md) |
| ② 抽取 | `trafilatura`、`resiliparse`、WET 文件 | [02](02-数据源与文本抽取.md) |
| ③ 过滤 | fastText langid、Gopher rules、C4 rules | [05](05-语言识别与启发式过滤.md) |
| ④ 去重 | MinHash-LSH、后缀数组、SemDeDup、Bloom filter | [06](06-去重.md) |
| ⑤ 质量分 | CCNet 困惑度、fastText 分类器、FineWeb-Edu 教育分 | [07](07-质量过滤.md) |
| ⑥ 去污染 | decontamination、n-gram overlap、PII 脱敏 | [08](08-去污染与隐私许可.md) |
| ⑦ 配比 | DoReMi、RegMix、data mixing law | [10](10-数据配比.md) |
| ⑦ 合成 | WRAP、phi/Cosmopedia、Nemotron-CC 改写 | [12](12-合成数据.md) |
| ⑧ 落盘 | Megatron `IndexedDatasetBuilder`、`preprocess_data.py` | [04](04-训练格式-Megatron索引.md) |
| ⑨ DataLoader | `GPTDataset`、`BlendedDataset`、packing、`cu_seqlens` | [13](13-打包与文档掩码.md)、[14](14-采样-shuffle-与断点续训.md) |

**几个"成品语料"的名字**（你会在技术报告里反复看到它们，[18 章](18-延伸阅读.md)有完整清单）：

| 语料 | 规模 | 一句话 | 论文 |
|---|---|---|---|
| **C4** | 156 B token | 2019 年的起点，规则过滤 | [1910.10683](https://arxiv.org/abs/1910.10683) |
| **The Pile** | 825 GB | 22 个来源人工拼起来的"杂食" | [2101.00027](https://arxiv.org/abs/2101.00027) |
| **RefinedWeb** | 5 T token | 证明了"纯网页也能打赢精选语料" | [2306.01116](https://arxiv.org/abs/2306.01116) |
| **Dolma** | 3 T token | 全流程完全开源可复现 | [2402.00159](https://arxiv.org/abs/2402.00159) |
| **FineWeb / FineWeb-Edu** | 15 T / 1.3 T | 现在的开源默认基线 | [2406.17557](https://arxiv.org/abs/2406.17557) |
| **DCLM-Baseline** | 3.8 T token | 把"数据处理"变成了一个可比赛的基准 | [2406.11794](https://arxiv.org/abs/2406.11794) |
| **Nemotron-CC** | 6.3 T token | 主张**放松过滤 + 合成补救** | [2412.02595](https://arxiv.org/abs/2412.02595) |
| **Essential-Web v1.0** | 24 T token | 不过滤，改成**给每篇打 12 类标签** | [2506.14111](https://arxiv.org/abs/2506.14111) |

---

## 1.9 小结

```
① 数据处理的目标：从 ~100 TB 原始网页里挑出 ~15 TB 值得学的
   ★ 丢掉 95%+ 是常态，不是做错了

② 九个环节被一条线劈成离线 / 在线两半，交接点是 .bin/.idx 文件
   离线：跑几周，落盘，可复用
   在线：每 step 几毫秒，不落盘，必须严格可复现

③ 三条因果链
   训练预算固定 → 垃圾 token 挤掉好 token → 过滤的收益是【机会成本】
   重复文本 → 重复学习 → 记忆/泄露/评测虚高 → 去重与去污染
   网页快用完 → 重复用 / 造新的 / 放松过滤靠配比补救

④ 一条数据的四种形态，务必分清：
   HTML  →  纯文本  →  JSON 记录  →  int32 数组  →  [B, L] 张量
    ①②        ②③④⑤⑥⑦              ⑧              ⑨

⑤ 一条训练样本里通常混着好几篇毫不相干的文档（packing），这是刻意的
```

---

> [目录](README.md) ｜ 下一章 → [02 数据源与文本抽取](02-数据源与文本抽取.md)
