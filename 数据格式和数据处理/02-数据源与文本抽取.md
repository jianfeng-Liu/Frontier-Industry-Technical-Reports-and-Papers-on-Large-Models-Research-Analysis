> [← 01 鸟瞰全景](01-鸟瞰全景.md) ｜ [目录](README.md) ｜ 下一章 → [03 存储格式](03-存储格式.md)

# 02 · 数据源与文本抽取

> **这一章回答两个问题：训练数据到底是从哪弄来的？抓下来的东西长什么样、怎么变成能用的文本？**

---

## 2.1 五个数据源，各自的角色

大模型的训练语料不是一个东西，是**五类来源按配比拌起来**的（配比怎么定见 [10 章](10-数据配比.md)）：

```
┌─ ① 网页（web） ─────────────────────────────────── 占 60~90% ─┐
│    来源：Common Crawl（免费）或自建爬虫（大厂几乎都自建）      │
│    特点：量最大、最脏、多样性最好                              │
│    角色：★ 语言能力和世界常识的主体                            │
└────────────────────────────────────────────────────────────┘
┌─ ② 代码（code） ──────────────────────────────── 占 5~20% ──┐
│    来源：GitHub 公开仓库（The Stack、StarCoder 数据集）        │
│    特点：结构严格、噪声低、许可证问题复杂                      │
│    角色：★ 不只是"会写代码"——公认能显著提升【推理能力】        │
└────────────────────────────────────────────────────────────┘
┌─ ③ 学术与书籍 ───────────────────────────────── 占 2~10% ──┐
│    来源：arXiv、PubMed、开放获取图书                          │
│    特点：长文档、逻辑连贯、密度高                              │
│    角色：★ 长上下文能力和专业知识                              │
└────────────────────────────────────────────────────────────┘
┌─ ④ 精选高质量 ────────────────────────────────── 占 1~5% ──┐
│    来源：维基百科、StackExchange、教科书                       │
│    特点：量小但质量极高，常被【重复采样多次】                  │
└────────────────────────────────────────────────────────────┘
┌─ ⑤ 合成（synthetic） ───────────────────────── 占 0~30% ──┐
│    来源：用别的模型生成或改写                                  │
│    ★ 2024 年后增长最快的一类，见 12 章                        │
└────────────────────────────────────────────────────────────┘
```

> ⚠️ 上面的百分比区间是我从各家公开语料配比里归纳的**量级区间**，不是任何一家的官方数字。真实配比往往是各家最不愿意公开的东西之一。

---

## 2.2 Common Crawl：这个领域的地基

**Common Crawl（CC）** 是一个非营利组织，**每一两个月对全网做一次爬取，并把结果免费公开**。几乎所有开源语料（C4、RefinedWeb、Dolma、FineWeb、DCLM、Nemotron-CC）都是从它出发的。

```
一次快照（一个 "dump"，如 CC-MAIN-2024-10）
   ≈ 25~30 亿个网页
   ≈ 90 TB 压缩后的数据
   
截至现在积累了 100 个左右的快照（2008 年至今）
   ★ 但快照之间【重复度极高】——同一个网页会在几十个快照里出现
      → 所以"用 100 个快照 = 100 倍数据"是错的，见 06 章去重
```

### CC 提供三种文件格式

这是初学者第一个容易踩的坑：**同一批网页，CC 提供三种加工程度不同的文件**。

```
┌─────────┬──────────────────────────┬─────────────┬─────────────┐
│ 格式    │ 里面装的是什么            │ 体积（单快照）│ 什么时候用   │
├─────────┼──────────────────────────┼─────────────┼─────────────┤
│ WARC    │ ★ 完整的原始 HTTP 响应    │  ~90 TB     │ 想自己抽正文 │
│         │   （HTTP 头 + 完整 HTML）  │             │ ★ 前沿做法   │
├─────────┼──────────────────────────┼─────────────┼─────────────┤
│ WAT     │ 元数据（链接、标题、头）  │  ~20 TB     │ 分析链接图   │
│         │   不含正文                │             │             │
├─────────┼──────────────────────────┼─────────────┼─────────────┤
│ WET     │ CC 帮你抽好的纯文本        │  ~9 TB      │ 图省事       │
│         │                          │             │ ★ 但质量差   │
└─────────┴──────────────────────────┴─────────────┴─────────────┘

WARC = Web ARChive         网页归档
WAT  = Web Archive Transformation  （元数据提取）
WET  = WARC Encapsulated Text      （纯文本提取）
```

> ★★ **这是一个真实存在的、影响很大的决策点。**
> **WET 的抽取质量很差**——它基本上就是"把所有 HTML 标签删掉"，所以导航栏、"登录 / 注册"、Cookie 提示、页脚版权全都留在正文里。
>
> ✅ **RefinedWeb 论文**（[arXiv:2306.01116](https://arxiv.org/abs/2306.01116)）明确报告：他们**从 WARC 出发用 `trafilatura` 自己抽正文，而不是用 WET**，并把这作为 RefinedWeb 质量领先的原因之一。**FineWeb 也走 WARC 路线。** 而更早的 **C4 用的是 WET**。
>
> ⚠️ 我的归纳：**"从 WET 换成 WARC + 好抽取器"，大概是 2020→2023 这几年数据质量提升里性价比最高的单点改动**——因为它不需要任何模型，只是换一个开源库。

---

## 2.3 ★ 文本抽取：为什么这一步决定了上限

### 直觉类比

想象你要从一份报纸上"只把新闻内容剪下来"。

- **WET 的做法**：把整版纸拿去，**把所有图片撕掉，剩下的字全留着**——包括报头、日期、广告、"第 3 版转下页"、订阅电话。
- **好抽取器的做法**：认出"这一块是正文，那一块是广告栏"，**只剪正文那一块**。

### 一个网页的字节都花在哪了

```
一个典型的现代网页（8 KB ~ 200 KB）：

  <script>  JS 代码            ████████████████████  ~40%
  <style> / class 属性          ██████████           ~20%
  导航 / 侧边栏 / 页脚          ████████             ~15%
  广告与追踪代码                ████                  ~8%
  ★ 正文                        ██████               ~12%
  其他标签                      ██                    ~5%

★ 也就是说，正文往往只占网页字节的 10~15%
   —— 这就是 01 章那张图里"抽取后剩 15%"的由来
```

### 三种抽取路线

```
① 规则/启发式（快，糙）
   删掉 <script> <style>，取 <p> 里的文字
   → WET 就是这一类

② 基于 DOM 密度的启发式（现在的主流）
   核心思想：★ 正文块的【文字/标签 比值】明显高于导航块
             正文块里逗号句号多，导航块里全是短词
   → trafilatura、resiliparse、jusText、Boilerpipe
   
③ 模型抽取（最准，最贵）
   训一个小模型判断每个 DOM 节点是不是正文
   → 大厂内部方案，⚠️ 公开细节很少
```

**②的核心指标写成公式**，对一个 HTML 节点：

```
文本密度(node) = 该节点子树里的【纯文本字符数】
                ────────────────────────────────
                 该节点子树里的【标签数量】

链接密度(node) = 该节点里【<a> 标签内的文字数】 / 【总文字数】

判定：文本密度高 且 链接密度低  →  正文
      文本密度低 或 链接密度高  →  导航 / 页脚 / 广告
                              ↑ 导航栏的特征就是"全是链接、每条几个字"
```

### 伪代码

```python
def extract_main_text(html):
    dom = parse_html(html)
    for node in dom.walk():
        if node.tag in ("script", "style", "noscript", "svg"):
            node.remove()                       # ① 先删非内容标签

    candidates = []
    for node in dom.walk():
        text  = node.text_content()
        n_tag = count_tags(node)
        n_link_char = sum(len(a.text) for a in node.find_all("a"))

        text_density = len(text) / max(n_tag, 1)
        link_density = n_link_char / max(len(text), 1)

        if len(text) > 100 and text_density > 20 and link_density < 0.3:
            candidates.append(node)             # ② 打分选块

    main = pick_largest_contiguous(candidates)  # ③ 选最大的连续正文区
    return normalize_whitespace(main.text_content())
```

> 实践里**不要自己写这个**，直接用 `trafilatura`（Python）或 `resiliparse`（更快，Rust/C++ 实现）。FineWeb 用的是 `trafilatura`。✅

---

## 2.4 网页以外的四类源，各有各的坑

### 代码

```
来源：GitHub 公开仓库（The Stack v1/v2、StarCoder Data）
坑：
  ① 许可证 —— 不是所有公开仓库都允许训练。要按 SPDX 许可证白名单过滤
  ② 自动生成的文件 —— .min.js、protobuf 生成的代码、lock 文件
     ★ 这些体积巨大且完全没有学习价值，必须按文件名和特征剔除
  ③ 一个仓库被 fork 一万次 —— 去重的重灾区
  ④ 二进制和大文件 —— 通常按"行平均长度""字母比例"过滤
```

### 学术论文（arXiv / PubMed）

```
来源：arXiv 提供 LaTeX 源码和 PDF 两种
★ 关键选择：用 LaTeX 源码，不要用 PDF
   PDF 抽文本会把两栏排版搅乱、公式变成乱码、表格塌成一行
   LaTeX 源码则可以精确地把公式保留成 $...$ 形式
坑：LaTeX 宏、\input 引用、参考文献列表（通常整段删掉）
```

### 书籍

```
坑：★ 版权是最大的问题。Books3 这个曾被广泛使用的语料因版权争议被下架
现在的合规做法：只用公有领域（Project Gutenberg）+ 明确开放许可的
参考：Common Pile 就是"只用明确开放许可的语料"这条路线的代表
```

### 维基百科与问答站

```
特点：量小（中英文全部加起来也就百亿 token 级别）但质量极高
常见做法：★ 重复采样 2~5 次（up-sampling），见 10 章
坑：Wiki 的模板、信息框（infobox）、引用标记 [1][2] 要清掉
```

---

## 2.5 ⚠️ 一件很少被讲、但很重要的事：自建爬虫

上面全在讲 Common Crawl，但**几乎所有前沿闭源模型都不只用 CC**。原因很实在：

```
Common Crawl 的局限：
  ① 它的爬取策略是通用的，不为"训练大模型"优化
     → 大量 PDF、图片页被抓了但没用，很多好站点抓得很浅
  ② 它遵守 robots.txt，而现在越来越多站点在 robots.txt 里禁止 AI 爬虫
     → ★ CC 的可用内容在【逐年缩水】
  ③ 所有人用的都是同一份 CC → 你的数据没有任何独特性
```

⚠️ **这是我的判断，不是某篇论文的结论**：数据侧的竞争差距，很大一部分已经不在"算法多聪明"，而在"你有没有自己的抓取管线和独家数据"。开源语料能追平的是前者，追不平后者。

---

## 2.6 挂回真实系统

| 语料 | 从哪抽 | 抽取器 | 出处 |
|---|---|---|---|
| **C4**（2019） | ✅ WET | CC 自带 | [1910.10683](https://arxiv.org/abs/1910.10683) |
| **RefinedWeb**（2023） | ✅ **WARC** | ✅ `trafilatura` | [2306.01116](https://arxiv.org/abs/2306.01116) |
| **Dolma**（2024） | WARC | 自研 + 规则 | [2402.00159](https://arxiv.org/abs/2402.00159) |
| **FineWeb**（2024） | ✅ **WARC** | ✅ `trafilatura` | [2406.17557](https://arxiv.org/abs/2406.17557) |
| **DCLM**（2024） | WARC | `resiliparse` | [2406.11794](https://arxiv.org/abs/2406.11794) |
| **Nemotron-CC**（2024） | WARC | `jusText` | [2412.02595](https://arxiv.org/abs/2412.02595) |

> ⚠️ 上表的"抽取器"一列，RefinedWeb 与 FineWeb 用 `trafilatura` 是论文明确写的；DCLM / Nemotron-CC 两栏是我依据其公开流程描述填写的，**建议以各自 PDF 为准**。

**五家国产模型的数据源披露程度**（详见 [16 章](16-五家报告的数据章节.md)）：⚠️ 总体而言，**这一代技术报告对数据源的披露远比对架构的披露吝啬**——架构会给出每一层的维度，数据往往只有一句"经过严格清洗的高质量多语言语料"。

---

## 2.7 小结

```
① 五类数据源：网页（主体）/ 代码（提推理）/ 学术书籍（长文档）
              / 精选高质量（重复采样）/ 合成（增长最快）

② Common Crawl 是地基。三种格式必须分清：
   WARC = 完整原始 HTML  ← ★ 前沿做法从这里出发
   WAT  = 元数据
   WET  = CC 帮你抽的纯文本  ← 图省事，但质量差（C4 用的这个）

③ ★ 正文只占一个网页字节的 10~15%，其余是 JS、CSS、导航、广告
   好抽取器的核心信号：文本密度高 + 链接密度低 = 正文

④ 实践建议：别自己写抽取器，用 trafilatura / resiliparse

⑤ 非网页源各有各的坑：
   代码 → 许可证 + 自动生成文件；论文 → 用 LaTeX 别用 PDF
   书籍 → 版权；Wiki → 量小质高，通常重复采样

⑥ ⚠️ CC 的可用内容在逐年缩水（robots.txt 拒绝 AI 爬虫），
   且人人都用同一份 → 前沿团队几乎都自建爬虫
```

---

> [← 01 鸟瞰全景](01-鸟瞰全景.md) ｜ [目录](README.md) ｜ 下一章 → [03 存储格式](03-存储格式.md)
