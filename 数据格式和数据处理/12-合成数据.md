> [← 11 喂多少：Scaling Law](11-喂多少-ScalingLaw.md) ｜ [目录](README.md) ｜ 下一章 → [13 打包与文档掩码](13-打包与文档掩码.md)

# 12 · 合成数据：数据不够就自己造

> **[11 章](11-喂多少-ScalingLaw.md)结尾给了三条出路，这一章讲第二条：用模型生成训练数据。**
>
> ★ 这是 2023 年之后**增长最快**、也是**争议最大**的一个方向。

---

## 12.1 先破除一个直觉障碍

新手听到"用模型生成数据来训模型"，第一反应通常是：

> **"这不是自己教自己吗？能有什么新信息？"**

这个疑问很合理，答案分三层：

```
① ★ 大多数合成数据不是"凭空造"，而是【改写已有数据】
   原料是真实网页的信息，模型只负责【换一种表达方式】
   ⟹ 信息量没变，但【形式变好了】—— 这就是价值

② ★★ 训练的瓶颈往往不是"信息量"，而是【信息的呈现形式】
   同一个知识点：
     · 埋在一篇有广告、有导航、语无伦次的网页里
     · 写成一段清晰的教科书式讲解
   ⟹ 后者的【学习效率】高得多。这不是新信息，但确实更好学。

③ ★★★ 在【可验证】的领域，合成数据能产生【真正的新信息】
   数学题：生成一道题 + 解法 → ★ 可以用求解器验证对不对
   代码：  生成一个函数 + 测试 → ★ 可以直接运行验证
   ⟹ 验证器提供了模型自身没有的【外部信号】
   ⟹ ★ 这就是为什么合成数据在数学和代码上效果最好、争议最小
```

> ★★ **记住这个三层结构**，本章后面所有方法都能归到这三类里。

---

## 12.2 两大范式

```
┌─ ① 改写型（source-rephrasing）───────────────────────────────┐
│    输入：一篇真实网页                                        │
│    动作：让模型换个风格重写                                  │
│    输出：内容相同、表达更好的文本                            │
│                                                             │
│    ✅ 优点：★ 事实来自原文，幻觉风险低；多样性由原始语料保证 │
│    ❌ 缺点：受限于原文的信息，上限就是原语料                │
│    代表：WRAP、Nemotron-CC                                  │
└─────────────────────────────────────────────────────────────┘

┌─ ② 生成型（generator-driven）───────────────────────────────┐
│    输入：一个主题 / 一个提示词 / 一组种子概念                │
│    动作：让模型从头写一篇                                    │
│    输出：全新的教科书、习题、对话                            │
│                                                             │
│    ✅ 优点：★ 质量上限高，可以精确控制难度和形式             │
│    ❌ 缺点：⚠️ 幻觉风险高；★★ 多样性【极难保证】             │
│    代表：phi 系列、Cosmopedia                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 12.3 改写型代表：WRAP

✅ **Rephrasing the Web (WRAP)**
（[arXiv:2401.16380](https://arxiv.org/abs/2401.16380)，Pratyush Maini 等，CMU / Apple，2024-01）

```
做法：用一个中等大小的开源指令模型，
      把 C4 的网页按【四种风格】各改写一遍：

   ① easy      —— ★ "像给小孩解释一样"（对应 07 章 DCLM 用 ELI5 当种子的同一直觉）
   ② medium    —— 维基百科风格
   ③ hard      —— 学术论文风格
   ④ Q/A       —— ★ 改写成一问一答的形式

★★ 为什么要【多种风格】而不是只用最好的那种？
   因为不同风格覆盖不同的下游能力，而且★多风格本身就是一种【多样性保护】
```

✅ 论文报告的结果：

```
· 在合成数据 + 真实数据混合训练下，
  ★ 预训练速度提升约 3 倍（达到同样效果所需的算力）
· 在 Pile 的多个子集上困惑度下降
· 零样本问答准确率提升约 10%
```

> ★ **WRAP 的关键设计**：**合成数据是和真实数据【混着用】的，不是替代。**
> 这一点后面 12.6 讲"模型坍塌"时会非常重要。

---

## 12.4 生成型代表：phi-1 与 Cosmopedia

### ✅ phi-1：Textbooks Are All You Need

（[arXiv:2306.11644](https://arxiv.org/abs/2306.11644)，Suriya Gunasekar 等，Microsoft，2023-06）

```
✅ 配置（★ 这组数字非常震撼，值得记住）：
   模型：1.3 B 参数（★ 极小）
   算力：8 张 A100，4 天
   数据：· 6 B token —— 从 The Stack + StackOverflow 里【筛出来】的
                       "教科书质量"的代码
                       ★ 筛选器是用 GPT-4 标注训的分类器（07 章第三代方法）
         · 1 B token —— ★ 用 GPT-3.5 生成的合成教科书与习题

✅ 结果：HumanEval pass@1 = 50.6%，MBPP = 55.5%
   ★ 用当时几十倍大的模型的水平，只花了 1.3 B 参数和 4 天
```

> ★★ **phi-1 的论点是"数据质量可以换算力和参数量"**，这在当时是很激进的。
>
> ⚠️ **必须同时知道的批评**：phi 系列一直被质疑**合成数据里可能间接混入了评测题的近亲**（[08 章](08-去污染与隐私许可.md)的间接污染）——用 GPT-3.5 生成"编程习题"，而 GPT-3.5 自己见过 HumanEval。**这个质疑至今没有被完全澄清。**
> ★ 所以 phi 的正确读法是：**它证明了这条路走得通，但具体分数要打折看。**

### Cosmopedia：★ 一个把"多样性问题"讲得最清楚的案例

```
HuggingFace 用 Mixtral-8x7B 生成的合成教科书语料（约 250 亿 token）。

★★ 它遇到的核心难题，正是生成型范式的命门：
   如果你就用"写一篇关于 X 的教科书"这个提示词，
   ⟹ 模型生成的几万篇文章会【高度雷同】：
      同样的开头、同样的结构、同样的例子

★ 它的解法：把提示词做成【组合爆炸】
   种子主题（几十万个，从真实语料里抽）
     × 受众（小学生 / 大学生 / 专业人士 / 完全外行）
     × 体裁（教科书章节 / 博客 / 维基条目 / 课堂讲稿 / 故事）
     × 风格
   ⟹ 每一篇的提示词都不同 → ★ 多样性由【提示词的多样性】来保证

★★★ 这引出了生成型范式最重要的一句话：
     【合成数据的多样性上限，由种子的多样性决定，不由生成模型决定。】
```

---

## 12.5 ★★★ Nemotron-CC：把两件事合起来的做法

✅ [07 章](07-质量过滤.md)讲过 Nemotron-CC（[2412.02595](https://arxiv.org/abs/2412.02595)）的分档过滤。它的合成数据用法**极其巧妙**，值得单独讲：

```
传统流水线：
   低质量数据  ──→  ★ 直接丢掉
   
Nemotron-CC：
   低质量数据  ──→  ★★ 交给模型【改写】成高质量  ──→  留下来用

⟹ 一石二鸟：
   ① 提升了这部分数据的质量
   ② ★ 保住了它的【唯一 token 量】—— 而这正是 11 章说的稀缺资源
```

✅ 论文报告：全量版本达到与 DCLM 相当的效果，但有**4 倍多的唯一 token**。

> ★★★ **这是本章我最想让你记住的一个思想转变：**
>
> ```
> 旧观念：过滤 = 一个【二分类】问题，好的留、坏的删
> 新观念：过滤 = 一个【分诊】问题
>         高质量 → 直接用
>         中质量 → 用
>         ★ 低质量 → 【改写后再用】，而不是删
>         垃圾   → 才删
> ```
>
> 这把 [07 章](07-质量过滤.md)的"质量过滤"和本章的"合成数据"缝成了一件事。

---

## 12.6 ⚠️ 三个真实的风险

### 风险一：★★ 模型坍塌（model collapse）

```
现象：如果【递归地】用模型生成的数据训下一代模型，
     一代一代下去，模型会逐渐【丢失分布的尾部】，
     最后收敛到一个方差极小、乏味的分布上。

直觉：模型生成时倾向于输出【高概率】的内容
     → 生成的数据里，罕见的表达、少数派观点、长尾知识被系统性削弱
     → 下一代模型在这个更窄的分布上训 → 更窄
     → ★ 像复印件的复印件

⚠️ 这个现象在 2024 年被广泛报道（含 Nature 上的工作）。
```

**但要把话说全**（⚠️ 这是我的判断）：

```
★ 坍塌实验的设定是【完全替换】：用生成数据【取代】真实数据，递归多轮。
  而实践中没有人这么干：

  ① 合成数据总是和真实数据【混着用】（WRAP 就是这样）
  ② 合成数据要经过和真实数据【一样的过滤和去重】
  ③ ★ 生成器通常比被训模型【更强】（用大模型给小模型造数据）
     —— 这是【蒸馏】，不是"自己教自己"

⟹ ★ 正确的结论不是"合成数据有毒"，而是：
   【合成数据的比例和多样性必须被主动管理，不能放任】
```

### 风险二：⚠️ 事实错误的规模化

```
模型改写时会【悄悄改错事实】：
   原文 "1969 年阿波罗 11 号登月"
   改写 "1968 年阿波罗 11 号登月"   ← ★ 流畅、自信、错误

★ 而且这个错误会被写成【教科书风格】—— 比原始网页的错误更有欺骗性，
  因为质量分类器（07 章）反而会给它【高分】。

★ 缓解：
  ① 改写时明确要求"不得引入原文没有的信息"
  ② 对数字、日期、专有名词做【原文回查】
  ③ ★ 在可验证领域用验证器（见下）
```

### 风险三：许可与合规

```
⚠️ 用某个闭源模型的输出去训练竞品模型，
   通常【违反其服务条款】。
★ 这是很多技术报告在合成数据部分语焉不详的真实原因之一。
```

---

## 12.7 ★★ 可验证领域：合成数据真正的甜区

```
★★★ 一个判断合成数据靠不靠谱的简单标准：
     【你能不能自动验证生成结果是对的？】

  能验证：
    数学   → 用符号计算/求解器验证；或生成时【先有答案再倒推出题】
    代码   → ★ 直接跑单元测试，跑不过就丢
    形式化证明 → 用证明检查器（Lean/Coq）验证
    结构化任务 → 用规则检查（JSON 格式、SQL 能否执行）

  不能验证：
    历史、常识、观点、开放式写作
    → ⚠️ 只能靠"改写型"（事实锚定在原文上），不要用"生成型"

⟹ ★★ 这解释了一个现象：
   为什么各家的合成数据几乎都集中在【数学、代码、推理】上？
   不是因为这些领域更重要，而是因为★【只有这些领域造得出可信的新数据】。
```

```python
# ★ 可验证合成的标准循环（以代码为例）
def synth_code_batch(seed_tasks, generator, n=8):
    kept = []
    for task in seed_tasks:
        for _ in range(n):                       # ★ 一题多次采样
            sol  = generator(f"实现：{task.desc}")
            test = generator(f"为下面的函数写单元测试：\n{sol}")
            if run_tests(sol, test) and run_tests(task.ref_impl, test):
                #  ↑ 生成解通过        ↑ ★ 参考实现也要通过，
                #                        否则说明是测试本身写错了
                kept.append({"task": task.desc, "solution": sol, "test": test})
                break
    return kept          # ★ 通过验证的才留下 —— 这一步提供了外部信号
```

---

## 12.8 挂回真实系统

| 工作 | 范式 | 做法 | 规模 / 效果 |
|---|---|---|---|
| **phi-1**（2023） | 生成型 | ✅ GPT-3.5 造教科书+习题 | ✅ 1.3B / HumanEval 50.6% |
| **WRAP**（2024） | ✅ 改写型 | ✅ 四种风格改写 C4 | ✅ 预训练提速约 3× |
| **Cosmopedia**（2024） | 生成型 | Mixtral-8x7B + ★ 提示词组合爆炸 | ~25 B token |
| **Nemotron-CC**（2024） | ✅ 改写型 | ★★ **低质档改写而非丢弃** | ✅ 唯一 token ×4 |
| **五家国产模型** | — | ⚠️ 多数只承认"使用了合成数据"，不给细节 | 见 [16 章](16-五家报告的数据章节.md) |

> ⚠️ **一个诚实的说明**：合成数据是当前技术报告里**披露最少的部分**——比配比还少。
> 原因既有竞争因素，也有 12.6 风险三提到的合规因素。
> ★ 所以本章的公开材料**主要来自学术论文，而不是前沿模型的技术报告**，读的时候要意识到这个偏差。

---

## 12.9 小结

```
① ★★ "自己教自己"的疑问，答案分三层：
   ① 多数合成是【改写】真实数据 → 信息没变，形式变好
   ② 训练瓶颈常常是【呈现形式】而非信息量
   ③ ★ 在【可验证】领域，验证器提供了真正的外部新信号

② 两大范式：
   改写型（WRAP / Nemotron-CC）—— ★ 事实锚在原文，幻觉低，上限受原语料限
   生成型（phi / Cosmopedia）—— 质量上限高，⚠️ 幻觉高、★★ 多样性极难保证

③ ✅ WRAP（2401.16380）：四种风格改写 C4（easy/medium/hard/QA）
   → ★ 预训练提速约 3 倍，零样本问答 +约 10%
   ★ 关键：合成数据是【混着真实数据用】，不是替代

④ ✅ phi-1（2306.11644）：1.3B / 8×A100 / 4 天 / 6B 筛选代码 + 1B 合成
   → HumanEval 50.6%
   ⚠️ 但一直被质疑合成数据里混入了评测题近亲（08 章的间接污染）
   ★ 读法：证明了路走得通，具体分数打折看

⑤ ★★★ Cosmopedia 的教训：
   【合成数据的多样性上限，由【种子】的多样性决定，不由生成模型决定】
   → 解法是提示词的组合爆炸（主题 × 受众 × 体裁 × 风格）

⑥ ★★★ Nemotron-CC 的思想转变（本章最重要）：
   过滤不是【二分类】（好留坏删），是【分诊】：
   高质量直接用 / 中质量用 / ★ 低质量【改写后再用】/ 垃圾才删
   ✅ 结果：唯一 token 量 ×4 —— 而这正是 11 章说的稀缺资源

⑦ ⚠️ 三个风险：
   模型坍塌（★ 但那是"完全替换 + 递归"的设定，实践中是混合 + 过滤 + 蒸馏）
   事实错误规模化（★ 而且改写后质量分类器反而给高分，更有欺骗性）
   合规（用闭源模型输出训竞品通常违反条款 —— 报告语焉不详的真实原因之一）

⑧ ★★ 判断合成数据靠不靠谱的唯一简单标准：【能不能自动验证】
   数学/代码/形式化证明 → 能验证 → 甜区
   历史/常识/观点      → 不能 → 只用改写型，别用生成型
   ⟹ 这就是为什么各家合成数据几乎都集中在数学和代码上
```

---

> [← 11 喂多少：Scaling Law](11-喂多少-ScalingLaw.md) ｜ [目录](README.md) ｜ 下一章 → [13 打包与文档掩码](13-打包与文档掩码.md)
